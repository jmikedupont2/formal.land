<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Formal Land Blog</title>
        <link>https://formal.land/blog</link>
        <description>Formal Land Blog</description>
        <lastBuildDate>Thu, 18 Jan 2024 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <item>
            <title><![CDATA[Upgrade the Rust version of coq-of-rust]]></title>
            <link>https://formal.land/blog/2024/01/18/update-coq-of-rust</link>
            <guid>/2024/01/18/update-coq-of-rust</guid>
            <pubDate>Thu, 18 Jan 2024 00:00:00 GMT</pubDate>
            <description><![CDATA[We continue our work on the coq-of-rust tool to formally verify Rust programs with the Coq proof assistant. We have upgraded the Rust version that we support, simplified the translation of the traits, and are adding better support for the standard library of Rust.]]></description>
            <content:encoded><![CDATA[<p>We continue our work on the <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> tool to formally verify Rust programs with the <a href="https://coq.inria.fr/">Coq proof assistant</a>. We have upgraded the Rust version that we support, simplified the translation of the traits, and are adding better support for the standard library of Rust.</p><p>Overall, we are now able to translate <strong>about 80%</strong> of the Rust examples from the <a href="https://doc.rust-lang.org/stable/rust-by-example/">Rust by Example</a> book into valid Coq files. This means we support a large subset of the Rust language.</p><p>:::tip Purchase</p><p>To formally verify your Rust codebase and improve the security of your application, email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>! Formal verification is the only way to prevent all bugs by exploring all possible executions of your programs<!-- --> <!-- -->🎯.</p><p>:::</p><p>:::info Thanks</p><p>This work and the development of <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> is made possible thanks to the <a href="https://alephzero.org/">Aleph Zero</a>&#x27;s Foundation, to develop an extra safe platform to build decentralized applications with formally verified smart contracts.</p><p>:::</p><p><img src="2024-01-18/rooster.png" alt="Rust rooster"/></p><h2>Upgrade of the Rust version</h2><p>The tool<!-- --> <code>coq-of-rust</code> is tied to a particular version of the Rust compiler that we use to parse and type-check a <code>cargo</code> project. We now support the<!-- --> <code>nightly-2023-12-15</code> version of Rust, up from<!-- --> <code>nightly-2023-04-30</code>. Most of the changes were minor, but it is good to handle these regularly to have smooth upgrades. The corresponding pull request is <a href="https://github.com/formal-land/coq-of-rust/pull/445">coq-of-rust/pull/445</a>. We also got more <a href="https://github.com/rust-lang/rust-clippy">Clippy</a> warnings thanks to the new version of Rust.</p><h2>Simplify the translation of traits</h2><p>The traits of Rust are similar to the <a href="https://coq.inria.fr/refman/addendum/type-classes.html">type-classes of Coq</a>. This is how we translate traits to Coq.</p><p>But there are a lot of subtle differences between the two languages. The type-class inference mechanism of Coq does not work all the time on generated Rust code, even when adding a lot of code annotations. We think that the only reliable way to translate Rust traits would be to explicit the implementations inferred by the Rust compiler, but the Rust compiler currently throws away this information.</p><p>Instead, our new solution is to use a Coq tactic:</p><pre><code class="language-coq">(** Try first to infer the trait instance, and if unsuccessful, delegate it at
    proof time. *)
Ltac get_method method :=
  exact (M.pure (method _)) ||
  exact (M.get_method method).
</code></pre><p>that first tries to infer the trait instance for a particular method, and if it fails, delegates its definition to the user at proof time. This is a bit unsafe, as a user could provide invalid instances at proof time, by giving its instance definitions instead of the ones generated by<!-- --> <code>coq-of-rust</code>. So, one should be careful to only apply generated instances to fill the hole made by this tactic in case of failure. We believe this to be a reasonable assumption that we could enforce someday if needed.</p><p>We are also starting to remove the trait constraints on polymorphic functions (the<!-- --> <code>where</code> clauses). We start by doing it in our manual definition of the standard library of Rust. The rationale is that we can provide the actual trait instances at proof time by having the right hypothesis replicating the constraints of the<!-- --> <code>where</code> clauses. Having fewer<!-- --> <code>where</code> clauses reduces the complexity of the type inference of Coq on the generated code. There are still some cases that we need to clarify, for example, the handling of <a href="https://doc.rust-lang.org/rust-by-example/generics/assoc_items/types.html">associated types</a> in the absence of traits.</p><h2>Handling more of the standard library</h2><p>We have a definition of the standard library of Rust, mainly composed of axiomatized<sup id="fnref-1"><a href="#fn-1" class="footnote-ref">1</a></sup> definitions, in these three folders:</p><ul><li><a href="https://github.com/formal-land/coq-of-rust/tree/main/CoqOfRust/alloc">CoqOfRust/alloc</a></li><li><a href="https://github.com/formal-land/coq-of-rust/tree/main/CoqOfRust/core">CoqOfRust/core</a></li><li><a href="https://github.com/formal-land/coq-of-rust/tree/main/CoqOfRust/std">CoqOfRust/std</a></li></ul><p>By adding more of these axioms, as well as with some small changes to the<!-- --> <code>coq-of-rust</code> tool, we are now able to successfully translate around 80% of the examples of the <a href="https://doc.rust-lang.org/stable/rust-by-example/">Rust by Example</a> book. There can still be some challenges on larger programs, but this showcases the good support of<!-- --> <code>coq-of-rust</code> for the Rust language.</p><h2>Conclusion</h2><p>We are continuing to improve our tool<!-- --> <code>coq-of-rust</code> to support more of the Rust language and are making good progress. If you need to improve the security of critical applications written in Rust, contact us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a> to start formally verifying your code!</p><div class="footnotes"><hr/><ol><li id="fn-1">An axiom in Coq is either a theorem whose proof is admitted, or a function/constant definition left for latter. This is the equivalent in Rust of the<!-- --> <code>todo!</code> macro.<a href="#fnref-1" class="footnote-backref">↩</a></li></ol></div>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Translating Rust match patterns to Coq with coq-of-rust]]></title>
            <link>https://formal.land/blog/2024/01/04/rust-translating-match</link>
            <guid>/2024/01/04/rust-translating-match</guid>
            <pubDate>Thu, 04 Jan 2024 00:00:00 GMT</pubDate>
            <description><![CDATA[Our tool coq-of-rust enables formal verification of 🦀&nbsp;Rust code to make sure that a program has no bugs. This technique checks all possible execution paths using mathematical techniques. This is important for example to ensure the security of smart contracts written in Rust language.]]></description>
            <content:encoded><![CDATA[<p>Our tool <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> enables <a href="https://en.wikipedia.org/wiki/Formal_verification">formal verification</a> of <a href="https://www.rust-lang.org/">🦀<!-- --> <!-- -->Rust</a> code to make sure that a program has no bugs. This technique checks all possible execution paths using mathematical techniques. This is important for example to ensure the security of smart contracts written in Rust language.</p><p>Our tool <code>coq-of-rust</code> works by translating Rust programs to the general proof system <a href="https://coq.inria.fr/">🐓<!-- --> <!-- -->Coq</a>. Here we explain how we translate<a href="https://doc.rust-lang.org/book/ch06-02-match.html"> <code>match</code> patterns</a> from Rust to Coq. The specificity of Rust patterns is to be able to match values either by value or reference.</p><p>:::tip Purchase</p><p>To formally verify your Rust codebase and improve the security of your application, email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>! Formal verification is the only way to prevent all bugs by exploring all possible executions of your program.</p><p>:::</p><p>:::info Thanks</p><p>This work and the development of <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> is made possible thanks to the <a href="https://alephzero.org/">Aleph Zero</a>&#x27;s Foundation, to develop an extra safe platform to build decentralized applications with formally verified smart contracts.</p><p>:::</p><p><img src="2024-01-04/rust-rooster.png" alt="Rust rooster"/></p><h2>Rust example<!-- --> <!-- -->🦀</h2><p>To illustrate the pattern matching in Rust, we will use the following example featuring a match by reference:</p><pre><code class="language-rust">pub(crate) fn is_option_equal&lt;A&gt;(
    is_equal: fn(x: &amp;A, y: &amp;A) -&gt; bool,
    lhs: Option&lt;A&gt;,
    rhs: &amp;A,
) -&gt; bool {
    match lhs {
        None =&gt; false,
        Some(ref value) =&gt; is_equal(value, rhs),
    }
}
</code></pre><p>We take a function<!-- --> <code>is_equal</code> as a parameter, operating only on references to the type<!-- --> <code>A</code>. We apply it to compare two values<!-- --> <code>lhs</code> and<!-- --> <code>rhs</code>:</p><ul><li>if<!-- --> <code>lhs</code> is<!-- --> <code>None</code>, we return<!-- --> <code>false</code>,</li><li>if<!-- --> <code>lhs</code> is<!-- --> <code>Some</code>, we get its value by reference and apply<!-- --> <code>is_equal</code>.</li></ul><p>When we apply the pattern:</p><pre><code class="language-rust">Some(ref value) =&gt; ...
</code></pre><p>we do something interesting: we read the value of<!-- --> <code>lhs</code> to know if we are in a<!-- --> <code>Some</code> case but leave it in place and return<!-- --> <code>value</code> the reference to its content.</p><p>To simulate this behavior in Coq, we need to match in two steps:</p><ol><li>match the value of<!-- --> <code>lhs</code> to know if we are in a<!-- --> <code>Some</code> case or not,</li><li>if we are in a<!-- --> <code>Some</code> case, create the reference to the content of a<!-- --> <code>Some</code> case based on the reference to<!-- --> <code>lhs</code>.</li></ol><h2>Coq translation<!-- --> <!-- -->🐓</h2><p>The Coq translation that our tool <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> generates is the following:</p><pre><code class="language-coq">Definition is_option_equal
    {A : Set}
    (is_equal : (ref A) -&gt; (ref A) -&gt; M bool.t)
    (lhs : core.option.Option.t A)
    (rhs : ref A)
    : M bool.t :=
  let* is_equal := M.alloc is_equal in
  let* lhs := M.alloc lhs in
  let* rhs := M.alloc rhs in
  let* α0 : M.Val bool.t :=
    match_operator
      lhs
      [
        fun γ =&gt;
          (let* α0 := M.read γ in
          match α0 with
          | core.option.Option.None =&gt; M.alloc false
          | _ =&gt; M.break_match
          end) :
          M (M.Val bool.t);
        fun γ =&gt;
          (let* α0 := M.read γ in
          match α0 with
          | core.option.Option.Some _ =&gt;
            let γ0_0 := γ.[&quot;Some.0&quot;] in
            let* value := M.alloc (borrow γ0_0) in
            let* α0 : (ref A) -&gt; (ref A) -&gt; M bool.t := M.read is_equal in
            let* α1 : ref A := M.read value in
            let* α2 : ref A := M.read rhs in
            let* α3 : bool.t := M.call (α0 α1 α2) in
            M.alloc α3
          | _ =&gt; M.break_match
          end) :
          M (M.Val bool.t)
      ] in
  M.read α0.
</code></pre><p>We run the<!-- --> <code>match_operator</code> on<!-- --> <code>lhs</code> and the two branches of the<!-- --> <code>match</code>. This operator is of type:</p><pre><code class="language-coq">Definition match_operator {A B : Set}
    (scrutinee : A)
    (arms : list (A -&gt; M B)) :
    M B :=
  ...
</code></pre><p>It takes a<!-- --> <code>scrutinee</code> value to match as a parameter, and runs a sequence of functions<!-- --> <code>arms</code> on it. Each function<!-- --> <code>arms</code> takes the value of the<!-- --> <code>scrutinee</code> and returns a monadic value<!-- --> <code>M B</code>. This monadic value can either be a success value if the pattern matches, or a special failure value if the pattern does not match. We evaluate the branches until one succeeds.</p><h3><code>None</code> branch</h3><p>The<!-- --> <code>None</code> branch is the simplest one. We read the value at the address given by<!-- --> <code>lhs</code> (we represent each Rust variable by its address) and match it with the<!-- --> <code>None</code> constructor:</p><pre><code class="language-coq">fun γ =&gt;
  (let* α0 := M.read γ in
  match α0 with
  | core.option.Option.None =&gt; M.alloc false
  | _ =&gt; M.break_match
  end) :
  M (M.Val bool.t)
</code></pre><p>If it matches, we return<!-- --> <code>false</code>. If it does not, we return the special value<!-- --> <code>M.break_match</code> to indicate that the pattern does not match.</p><h3><code>Some</code> branch</h3><p>In the<!-- --> <code>Some</code> branch, we first also read the value at the address given by<!-- --> <code>lhs</code> and match it with the<!-- --> <code>Some</code> constructor:</p><pre><code class="language-coq">fun γ =&gt;
  (let* α0 := M.read γ in
  match α0 with
  | core.option.Option.Some _ =&gt;
    let γ0_0 := γ.[&quot;Some.0&quot;] in
    let* value := M.alloc (borrow γ0_0) in
    let* α0 : (ref A) -&gt; (ref A) -&gt; M bool.t := M.read is_equal in
    let* α1 : ref A := M.read value in
    let* α2 : ref A := M.read rhs in
    let* α3 : bool.t := M.call (α0 α1 α2) in
    M.alloc α3
  | _ =&gt; M.break_match
  end) :
  M (M.Val bool.t)
</code></pre><p>If we are in that case, we create the value:</p><pre><code class="language-coq">let γ0_0 := γ.[&quot;Some.0&quot;] in
</code></pre><p>with the address of the first field of the<!-- --> <code>Some</code> constructor, relative to the address of<!-- --> <code>lhs</code> given in<!-- --> <code>γ</code>. We define the operator<!-- --> <code>.[&quot;Some.0&quot;]</code> when we define the option type and generate such definitions for all user-defined enum types.</p><p>We then encapsulate the address<!-- --> <code>γ0_0</code> in a proper Rust reference:</p><pre><code class="language-coq">let* value := M.alloc (borrow γ0_0) in
</code></pre><p>of type<!-- --> <code>ref A</code> in the original Rust code. Finally, we call the function<!-- --> <code>is_equal</code> on the two references<!-- --> <code>value</code> and<!-- --> <code>rhs</code>, with some boilerplate code to read and allocate the variables.</p><h2>General translation</h2><p>We generalize this translation to all patterns by:</p><ul><li>flattening all the or patterns <code>|</code> so that only patterns with a single choice remain,</li><li>evaluating each match branch in order with the<!-- --> <code>match_operator</code> operator,</li><li>in each branch, evaluating the inner patterns in order. This evaluation might fail at any point if the pattern does not match. In this case, we return the special value<!-- --> <code>M.break_match</code> and continue with the next branch.</li></ul><p>At least one branch should succeed as the Rust compiler checks that all cases are covered. We still have a special value<!-- --> <code>M.impossible</code> in Coq for the case where no patterns match and satisfy the type checker.</p><p>We distinguish and handle the following kind of patterns (and all their combinations):</p><ul><li>wild patterns<!-- --> <code>_</code>,</li><li>binding patterns<!-- --> <code>(ref) name</code> or<!-- --> <code>(ref) name as pattern</code> (the<!-- --> <code>ref</code> keyword is optional),</li><li>struct patterns<!-- --> <code>Name { field1: pattern1, ... }</code> or<!-- --> <code>Name(pattern1, ...)</code></li><li>tuple patterns<!-- --> <code>(pattern1, ...)</code>,</li><li>literal patterns<!-- --> <code>12</code>, <code>true</code>, ...,</li><li>slice patterns<!-- --> <code>[first, second, tail @ ..]</code>,</li><li>dereference patterns<!-- --> <code>&amp;pattern</code>.</li></ul><p>This was enough to cover all of our examples. The Rust compiler can also automatically add some<!-- --> <code>ref</code> patterns when matching on references. We do not need to handle this case as this is automatically done by the Rust compiler during its compilation to the intermediate<!-- --> <a href="https://rustc-dev-guide.rust-lang.org/thir.html">THIR</a> representation, and e directly read the THIR code.</p><h2>Conclusion</h2><p>In this blog post, we have presented how we translate Rust patterns to the proof system Coq. The difficult part is handling the<!-- --> <code>ref</code> patterns, which we do by matching in two steps: matching on the values and then computing the addresses of the sub-fields.</p><p>If you have Rust smart contracts or programs to verify, feel free to email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>. We will be happy to help!</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Verifying an ERC-20 smart contract in Rust]]></title>
            <link>https://formal.land/blog/2023/12/13/rust-verify-erc-20-smart-contract</link>
            <guid>/2023/12/13/rust-verify-erc-20-smart-contract</guid>
            <pubDate>Wed, 13 Dec 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[Our tool coq-of-rust enables formal verification of 🦀&nbsp;Rust code to make sure that a program has no bugs given a precise specification. We work by translating Rust programs to the general proof system 🐓&nbsp;Coq.]]></description>
            <content:encoded><![CDATA[<p>Our tool <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> enables formal verification of <a href="https://www.rust-lang.org/">🦀<!-- --> <!-- -->Rust</a> code to make sure that a program has no bugs given a precise specification. We work by translating Rust programs to the general proof system <a href="https://coq.inria.fr/">🐓<!-- --> <!-- -->Coq</a>.</p><p>Here, we show how we formally verify an <a href="https://github.com/paritytech/ink/blob/master/integration-tests/erc20/lib.rs">ERC-20 smart contract</a> written in Rust for the <a href="https://alephzero.org/">Aleph Zero</a> blockchain. <a href="https://en.wikipedia.org/wiki/Ethereum#ERC20">ERC-20</a> smart contracts are used to create new kinds of tokens in an existing blockchain. Examples are stablecoins such as the <a href="https://tether.to/">💲USDT</a>.</p><p>:::tip Purchase</p><p>To formally verify your Rust codebase and improve the security of your application, email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>! Formal verification is the only way to prevent all bugs by exploring all possible executions of your program.</p><p>:::</p><p>:::info Thanks</p><p>This work and the development of <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> is made possible thanks to the <a href="https://alephzero.org/">Aleph Zero</a>&#x27;s Foundation, to develop an extra safe platform to build decentralized applications with formally verified smart contracts.</p><p>:::</p><p><img src="2023-12-13/rooster-verifying.png" alt="Rooster verifying"/></p><h2>Smart contract code<!-- --> <!-- -->🦀</h2><p>Here is the Rust code of the smart contract that we want to verify:</p><pre><code class="language-rust">#[ink::contract]
mod erc20 {
    use ink::storage::Mapping;

    #[ink(storage)]
    #[derive(Default)]
    pub struct Erc20 {
        total_supply: Balance,
        balances: Mapping&lt;AccountId, Balance&gt;,
        allowances: Mapping&lt;(AccountId, AccountId), Balance&gt;,
    }

    #[ink(event)]
    pub struct Transfer {
        // ...
    }

    #[ink(event)]
    pub struct Approval {
        // ...
    }

    #[derive(Debug, PartialEq, Eq)]
    #[ink::scale_derive(Encode, Decode, TypeInfo)]
    pub enum Error {
        // ...
    }

    pub type Result&lt;T&gt; = core::result::Result&lt;T, Error&gt;;

    impl Erc20 {
        #[ink(constructor)]
        pub fn new(total_supply: Balance) -&gt; Self {
            let mut balances = Mapping::default();
            let caller = Self::env().caller();
            balances.insert(caller, &amp;total_supply);
            Self::env().emit_event(Transfer {
                from: None,
                to: Some(caller),
                value: total_supply,
            });
            Self {
                total_supply,
                balances,
                allowances: Default::default(),
            }
        }

        #[ink(message)]
        pub fn total_supply(&amp;self) -&gt; Balance {
            self.total_supply
        }

        #[ink(message)]
        pub fn balance_of(&amp;self, owner: AccountId) -&gt; Balance {
            self.balance_of_impl(&amp;owner)
        }

        #[inline]
        fn balance_of_impl(&amp;self, owner: &amp;AccountId) -&gt; Balance {
            self.balances.get(owner).unwrap_or_default()
        }

        #[ink(message)]
        pub fn allowance(&amp;self, owner: AccountId, spender: AccountId) -&gt; Balance {
            self.allowance_impl(&amp;owner, &amp;spender)
        }

        #[inline]
        fn allowance_impl(&amp;self, owner: &amp;AccountId, spender: &amp;AccountId) -&gt; Balance {
            self.allowances.get((owner, spender)).unwrap_or_default()
        }

        #[ink(message)]
        pub fn transfer(&amp;mut self, to: AccountId, value: Balance) -&gt; Result&lt;()&gt; {
            let from = self.env().caller();
            self.transfer_from_to(&amp;from, &amp;to, value)
        }

        #[ink(message)]
        pub fn approve(&amp;mut self, spender: AccountId, value: Balance) -&gt; Result&lt;()&gt; {
            let owner = self.env().caller();
            self.allowances.insert((&amp;owner, &amp;spender), &amp;value);
            self.env().emit_event(Approval {
                owner,
                spender,
                value,
            });
            Ok(())
        }

        #[ink(message)]
        pub fn transfer_from(
            &amp;mut self,
            from: AccountId,
            to: AccountId,
            value: Balance,
        ) -&gt; Result&lt;()&gt; {
            let caller = self.env().caller();
            let allowance = self.allowance_impl(&amp;from, &amp;caller);
            if allowance &lt; value {
                return Err(Error::InsufficientAllowance)
            }
            self.transfer_from_to(&amp;from, &amp;to, value)?;
            // We checked that allowance &gt;= value
            #[allow(clippy::arithmetic_side_effects)]
            self.allowances
                .insert((&amp;from, &amp;caller), &amp;(allowance - value));
            Ok(())
        }

        fn transfer_from_to(
            &amp;mut self,
            from: &amp;AccountId,
            to: &amp;AccountId,
            value: Balance,
        ) -&gt; Result&lt;()&gt; {
            let from_balance = self.balance_of_impl(from);
            if from_balance &lt; value {
                return Err(Error::InsufficientBalance)
            }
            // We checked that from_balance &gt;= value
            #[allow(clippy::arithmetic_side_effects)]
            self.balances.insert(from, &amp;(from_balance - value));
            let to_balance = self.balance_of_impl(to);
            self.balances
                .insert(to, &amp;(to_balance.checked_add(value).unwrap()));
            self.env().emit_event(Transfer {
                from: Some(*from),
                to: Some(*to),
                value,
            });
            Ok(())
        }
    }
}
</code></pre><p>This whole code is rather short and contains no loops, which will simplify our verification process. It uses a lot of macros, such as <code>#[ink(message)]</code>, that are specific to the <a href="https://use.ink/">ink!</a> language for smart contracts, built on top of Rust. To verify this smart contract, we removed all the macros and added a mock of the dependencies, such as <code>ink::storage::Mapping</code> to get a map data structure.</p><h2>The Coq translation<!-- --> <!-- -->🐓</h2><p>By running our tool <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> we automatically obtain the corresponding Coq code for the contract <a href="https://github.com/formal-land/coq-of-rust/blob/main/CoqOfRust/examples/default/examples/ink_contracts/erc20.v">erc20.v</a>. Here is an extract for the <code>transfer</code> function:</p><pre><code class="language-coq">(*
    fn transfer(&amp;mut self, to: AccountId, value: Balance) -&gt; Result&lt;()&gt; {
        let from = self.env().caller();
        self.transfer_from_to(&amp;from, &amp;to, value)
    }
*)
Definition transfer
    (self : mut_ref ltac:(Self))
    (to : erc20.AccountId.t)
    (value : ltac:(erc20.Balance))
    : M ltac:(erc20.Result unit) :=
  let* self : M.Val (mut_ref ltac:(Self)) := M.alloc self in
  let* to : M.Val erc20.AccountId.t := M.alloc to in
  let* value : M.Val ltac:(erc20.Balance) := M.alloc value in
  let* from : M.Val erc20.AccountId.t :=
    let* α0 : mut_ref erc20.Erc20.t := M.read self in
    let* α1 : erc20.Env.t :=
      M.call (erc20.Erc20.t::[&quot;env&quot;] (borrow (deref α0))) in
    let* α2 : M.Val erc20.Env.t := M.alloc α1 in
    let* α3 : erc20.AccountId.t :=
      M.call (erc20.Env.t::[&quot;caller&quot;] (borrow α2)) in
    M.alloc α3 in
  let* α0 : mut_ref erc20.Erc20.t := M.read self in
  let* α1 : u128.t := M.read value in
  let* α2 : core.result.Result.t unit erc20.Error.t :=
    M.call
      (erc20.Erc20.t::[&quot;transfer_from_to&quot;] α0 (borrow from) (borrow to) α1) in
  let* α0 : M.Val (core.result.Result.t unit erc20.Error.t) := M.alloc α2 in
  M.read α0.
</code></pre><p>More details of the translation are given in previous blog posts, but basically:</p><ul><li>we make explicit all memory and implicit operations (like borrowing and dereferencing),</li><li>we apply a monadic translation to chain the primitive operations with <code>let*</code>.</li></ul><h2>Proof strategy</h2><p><img src="2023-12-13/proof-strategy.png" alt="Proof strategy"/></p><p>We verify the code in two steps:</p><ol><li>Show that a simpler, purely functional Coq code can simulate all the smart contract code.</li><li>Show that the simulation is correct.</li></ol><p>That way, we can eliminate all the memory-related operations by showing the equivalence with a simulation. Then, we can focus on the functional code, which is more straightforward to reason about. We can cite another project, <a href="https://github.com/AeneasVerif/aeneas">Aeneas</a>, which proposes to do the first step (removing memory operations) automatically.</p><h2>Simulations</h2><h3>Simulation code</h3><p>We will work on the example of the <code>transfer</code> function. We define the simulations in <a href="https://github.com/formal-land/coq-of-rust/blob/main/CoqOfRust/examples/default/examples/ink_contracts/Simulations/erc20.v">Simulations/erc20.v</a>. For the <code>transfer</code> function this is:</p><pre><code class="language-coq">Definition transfer
    (env : erc20.Env.t)
    (to : erc20.AccountId.t)
    (value : ltac:(erc20.Balance)) :
    MS? State.t ltac:(erc20.Result unit) :=
  transfer_from_to (Env.caller env) to value.
</code></pre><p>The function <code>transfer</code> is a wrapper around <code>transfer_from_to</code>, using the smart contract caller as the <code>from</code> account. The monad <code>MS?</code> combines the state and error effect. The state is given by the <code>State.t</code> type:</p><pre><code class="language-coq">Module State.
  Definition t : Set := erc20.Erc20.t * list erc20.Event.t.
End State.
</code></pre><p>It combines the state of the contract (type <code>Self</code> in the Rust code) and a list of events to represent the logs. The errors of the monad include panic errors, as well as control flow primitives such as <code>return</code> or <code>break</code> that we implement with exceptions.</p><h3>Equivalence statement</h3><p>We write all our proofs in <a href="https://github.com/formal-land/coq-of-rust/blob/main/CoqOfRust/examples/default/examples/ink_contracts/Proofs/erc20.v">Proofs/erc20.v</a>. The lemma stating that the simulation is equivalent to the original code is:</p><pre><code class="language-coq">Lemma run_transfer
    (env : erc20.Env.t)
    (storage : erc20.Erc20.t)
    (to : erc20.AccountId.t)
    (value : ltac:(erc20.Balance))
    (H_storage : Erc20.Valid.t storage)
    (H_value : Integer.Valid.t value) :
  let state := State.of_storage storage in
  let self := Ref.mut_ref Address.storage in
  let simulation :=
    lift_simulation
      (Simulations.erc20.transfer env to value) storage in
  {{ Environment.of_env env, state |
    erc20.Impl_erc20_Erc20_t_2.transfer self to value ⇓
    simulation.(Output.result)
  | simulation.(Output.state) }}.
</code></pre><p>The main predicate is:</p><pre><code class="language-coq">{{ env, state | translated_code ⇓ result | final_state }}.
</code></pre><p>This predicate defines our semantics, explaining how to evaluate a translated Rust code in an environment <code>env</code> and a state <code>state</code>, to obtain a result <code>result</code> and a final state <code>final_state</code>. We use an environment in addition to a state to initialize various globals and other information related to the execution context. For example, here, we use the environment to store the <code>caller</code> of the contract and the pointer to the list of logs.</p><h3>Semantics</h3><p>We define our monad for the translated code <code>M A</code> in a style by continuation:</p><pre><code class="language-coq">Inductive t (A : Set) : Set :=
| Pure : A -&gt; t A
| CallPrimitive {B : Set} : Primitive.t B -&gt; (B -&gt; t A) -&gt; t A
| Cast {B1 B2 : Set} : B1 -&gt; (B2 -&gt; t A) -&gt; t A
| Impossible : t A.
Arguments Pure {_}.
Arguments CallPrimitive {_ _}.
Arguments Cast {_ _ _}.
Arguments Impossible {_}.
</code></pre><p>For now, we use the primitives to access the memory and the environment:</p><pre><code class="language-coq">Module Primitive.
  Inductive t : Set -&gt; Set :=
  | StateAlloc {A : Set} : A -&gt; t (Ref.t A)
  | StateRead {Address A : Set} : Address -&gt; t A
  | StateWrite {Address A : Set} : Address -&gt; A -&gt; t unit
  | EnvRead {A : Set} : t A.
End Primitive.
</code></pre><p>For each of our monad constructs, we add a case to our evaluation predicate that we will describe:</p><ul><li><code>Pure</code> The result is the value itself, and the state is unchanged:<pre><code class="language-coq">| Pure :
  {{ env, state&#x27; | LowM.Pure result ⇓ result | state&#x27; }}
</code></pre></li><li><code>Cast</code> The evaluation is only possible when <code>B1</code> and <code>B2</code> are the same type <code>B</code>:<pre><code class="language-coq">| Cast {B : Set} (state : State) (v : B) (k : B -&gt; LowM A) :
  {{ env, state | k v ⇓ result | state&#x27; }} -&gt;
  {{ env, state | LowM.Cast v k ⇓ result | state&#x27; }}
</code></pre>In this case, we return the result of the continuation <code>k</code> of the cast. We do not change the state in the cast.</li><li>We read the state using the primitive <code>State.read</code>, checking that the <code>address</code> is indeed allocated (it returns <code>None</code> otherwise). Note that the type of <code>v</code> depends on its address. We directly allocate values with their original type, to avoid serializations/deserializations to represent the state.<pre><code class="language-coq">| CallPrimitiveStateRead
    (address : Address) (v : State.get_Set address)
    (state : State)
    (k : State.get_Set address -&gt; LowM A) :
  State.read address state = Some v -&gt;
  {{ env, state | k v ⇓ result | state&#x27; }} -&gt;
  {{ env, state |
    LowM.CallPrimitive (Primitive.StateRead address) k ⇓ result
  | state&#x27; }}
</code></pre></li><li>Similarly, we write into the state with <code>State.alloc_write</code>, that only succeeds for allocated addresses:<pre><code class="language-coq">| CallPrimitiveStateWrite
    (address : Address) (v : State.get_Set address)
    (state state_inter : State)
    (k : unit -&gt; LowM A) :
  State.alloc_write address state v = Some state_inter -&gt;
  {{ env, state_inter | k tt ⇓ result | state&#x27; }} -&gt;
  {{ env, state |
    LowM.CallPrimitive (Primitive.StateWrite address v) k ⇓ result
  | state&#x27; }}
</code></pre></li><li>To allocate a new value in memory, we have to make a choice depending on whether we want this value to be writable or not. For immutable values, we do not create a new address and instead say that the address is the value itself:<pre><code class="language-coq">| CallPrimitiveStateAllocNone {B : Set}
    (state : State) (v : B)
    (k : Ref B -&gt; LowM A) :
  {{ env, state | k (Ref.Imm v) ⇓ result | state&#x27; }} -&gt;
  {{ env, state |
    LowM.CallPrimitive (Primitive.StateAlloc v) k ⇓ result
  | state&#x27; }}
</code></pre>If we later attempt to update this value, it will not be possible to define a semantics and we will be stuck. It is up to the user to correctly anticipate if a value will be updated or not to define the semantics. For values that might be updated, we use:<pre><code class="language-coq">| CallPrimitiveStateAllocSome
    (address : Address) (v : State.get_Set address)
    (state : State)
    (k : Ref (State.get_Set address) -&gt; LowM A) :
  let r :=
    Ref.MutRef (A := State.get_Set address) (B := State.get_Set address)
      address (fun full_v =&gt; full_v) (fun v _full_v =&gt; v) in
  State.read address state = None -&gt;
  State.alloc_write address state v = Some state&#x27; -&gt;
  {{ env, state | k r ⇓ result | state&#x27; }} -&gt;
  {{ env, state |
    LowM.CallPrimitive (Primitive.StateAlloc v) k ⇓ result
  | state&#x27; }}
</code></pre>We need to provide an address not already allocated: <code>State.read</code> should return <code>None</code>. At this point, we can make any choice of unallocated address in order to simplify the proofs later.</li><li>Finally, we read the whole environment with:<pre><code class="language-coq">| CallPrimitiveEnvRead
    (state : State) (k : Env -&gt; LowM A) :
  {{ env, state | k env ⇓ result | state&#x27; }} -&gt;
  {{ env, state |
    LowM.CallPrimitive Primitive.EnvRead k ⇓ result
  | state&#x27; }}
</code></pre></li></ul><h3>Semantics remarks</h3><p>We can make a few remarks about our semantics:</p><ul><li>There are no cases for <code>M.Impossible</code> as this primitive corresponds to impossible branches in the code.</li><li>The semantics is not computable, in the sense that we cannot define a function <code>run</code> to evaluate a monadic program in a certain environment and state. Indeed, the user needs to make a choice during the allocation of new values, to know if we allocate the value as immutable or mutable, and with which address. The <code>M.Cast</code> operator is also not computable, as we cannot decide if two types are equal.</li><li>We can choose the type that we use for the <code>State</code>, as well as the primitives <code>State.read</code> and <code>State.alloc_write</code>, as long as they verify well-formedness properties. For example, reading after a write at the same address should return the written value. One should choose a <code>State</code> that simplifies its proofs the most. To verify the smart contract, we have taken a record with two fields:<ol><li>the storage of the contract (the <code>Self</code> type in Rust),</li><li>the list of events logged by the contract.</li></ol></li><li>Even if the monad is in continuation-passing style, we add a primitive <code>M.Call</code> corresponding to a bind, to explicit the points in the code where we call user-defined functions. This is not necessary but helpful to track things in the proofs. Otherwise, the monadic bind is defined as a fixpoint with:<pre><code class="language-coq">Fixpoint bind {A B : Set} (e1 : t A) (f : A -&gt; t B) : t B :=
  match e1 with
  | Pure v =&gt; f v
  | CallPrimitive primitive k =&gt;
    CallPrimitive primitive (fun v =&gt; bind (k v) f)
  | Cast v k =&gt;
    Cast v (fun v&#x27; =&gt; bind (k v&#x27;) f)
  | Impossible =&gt; Impossible
  end.
</code></pre></li><li>To handle the panic and <code>return</code>/<code>break</code> exceptions, we wrap our monad into an error monad:<pre><code class="language-coq">Definition M (A : Set) : Set :=
  LowM (A + Exception.t).
</code></pre>where <code>LowM</code> is the monad without errors as defined above and <code>Exception.t</code> is:<pre><code class="language-coq">Module Exception.
  Inductive t : Set :=
  (** exceptions for Rust&#x27;s `return` *)
  | Return {A : Set} : A -&gt; t
  (** exceptions for Rust&#x27;s `continue` *)
  | Continue : t
  (** exceptions for Rust&#x27;s `break` *)
  | Break : t
  | Panic : Coq.Strings.String.string -&gt; t.
End Exception.
</code></pre></li></ul><h3>Proof of equivalence</h3><p>To prove that the equivalence between the simulation and the original code holds, we proceed by induction on the monadic code. This corresponds to symbolically evaluating the monadic code, in the proof mode of Coq, applying the primitives of the semantics predicate at each step. We use the following tactic to automate this work:</p><pre><code class="language-coq">run_symbolic.
</code></pre><p>We manually handle the following cases:</p><ul><li>branching (<code>if</code> or <code>match</code>),</li><li>external function calls: generally, we apply an existing equivalence proof for a call to another function instead of doing the symbolic evaluation of the function,</li><li>memory allocations: we need to choose the type of allocation (mutable or immutable) and the address of the allocation for mutable ones.</li></ul><p>Here is the proof for the <code>transfer</code> function:</p><pre><code class="language-coq">Proof.
  unfold erc20.Impl_erc20_Erc20_t_2.transfer,
    Simulations.erc20.transfer,
    lift_simulation.
  Opaque erc20.transfer_from_to.
  run_symbolic.
  eapply Run.Call. {
    apply run_env.
  }
  run_symbolic.
  eapply Run.Call. {
    apply Env.run_caller.
  }
  run_symbolic.
  eapply Run.Call. {
    now apply run_transfer_from_to.
  }
  unfold lift_simulation.
  destruct erc20.transfer_from_to as [[] [?storage ?logs]]; run_symbolic.
  Transparent erc20.transfer_from_to.
Qed.
</code></pre><h2>Proofs</h2><h3>Handling of integers</h3><p>We distinguish the various types of integers used in Rust:</p><ul><li>unsigned ones: <code>u8</code>, <code>u16</code>, <code>u32</code>, <code>u64</code>, <code>u128</code>, <code>usize</code>,</li><li>signed ones: <code>i8</code>, <code>i16</code>, <code>i32</code>, <code>i64</code>, <code>i128</code>, <code>isize</code>.</li></ul><p>We define a separate type for each of them, that is to say, a wrapper around the <code>Z</code> type of unbounded integers from Coq:</p><pre><code class="language-coq">Module u8.
  Inductive t : Set := Make (z : Z) : t.
End u8.
</code></pre><p>To enforce the bounds, we define a validity predicate for each type:</p><pre><code class="language-coq">Module Valid.
  Definition t {A : Set} `{Integer.C A} (v : A) : Prop :=
    Integer.min &lt;= Integer.to_Z v &lt;= Integer.max.
End Valid.
</code></pre><p>All integer types are of the class <code>Integer.C</code> with a <code>min</code>, <code>max</code>, and <code>to_Z</code> functions. We do not embed this predicate with the integer type (<a href="https://en.wikipedia.org/wiki/Refinement_type">refinement type</a>) to avoid mixing proofs and code. We pay a cost by having to handle the values and the validity proofs separately.</p><p>Depending on the configuration mode of Rust, integer operations can overflow or panic. We have several implementations of the arithmetic operations, depending on the mode:</p><pre><code class="language-coq">Module BinOp.
  (** Operators with panic, in the monad. *)
  Module Panic.
    Definition add {A : Set} `{Integer.C A} (v1 v2 : A) : M A :=
      (* ... *)

    Definition sub (* ... *)
  End Panic.

  (** Operators with overflow, outside of the monad as
      there cannot be any errors. *)
  Module Wrap.
    Definition add {A : Set} `{Integer.C A} (v1 v2 : A) : A :=
      (* ... *)

    Definition sub (* ... *)
  End Wrap.
End BinOp.
</code></pre><p>We also have additional operators, useful for the definition of simulations:</p><ul><li>optimistic operators, operating on <code>Z</code> without checking the bounds of the result (for cases where we can prove that the result is never out of bounds),</li><li>operators returning in the option monad, to handle the case where the result is out of bounds.</li></ul><p>Note that the comparison operators (<code>=</code>, <code>&lt;</code>, ...) never panic or overflow. In the context of these smart contracts, the arithmetic operators are panicking in case of overflow.</p><h3>Definition of messages</h3><p>We can call the smart contract with three read primitives (<code>total_supply</code>, <code>balance_of</code>, <code>allowance</code>) and three write primitives (<code>transfer</code>, <code>approve</code>, <code>transfer_from</code>). We define two message types to formalize these access points. This will later allow us to express properties over all possible read and write messages:</p><pre><code class="language-coq">Module ReadMessage.
  (** The type parameter is the type of result of the call. *)
  Inductive t : Set -&gt; Set :=
  | total_supply :
    t ltac:(erc20.Balance)
  | balance_of
    (owner : erc20.AccountId.t) :
    t ltac:(erc20.Balance)
  | allowance
    (owner : erc20.AccountId.t)
    (spender : erc20.AccountId.t) :
    t ltac:(erc20.Balance).
End ReadMessage.

Module WriteMessage.
  Inductive t : Set :=
  | transfer
    (to : erc20.AccountId.t)
    (value : ltac:(erc20.Balance)) :
    t
  | approve
    (spender : erc20.AccountId.t)
    (value : ltac:(erc20.Balance)) :
    t
  | transfer_from
    (from : erc20.AccountId.t)
    (to : erc20.AccountId.t)
    (value : ltac:(erc20.Balance)) :
    t.
End WriteMessage.
</code></pre><h3>No panics on read messages</h3><p>We show that for all possible read messages, the smart contract does not panic:</p><pre><code class="language-coq">Lemma read_message_no_panic
    (env : erc20.Env.t)
    (message : ReadMessage.t ltac:(erc20.Balance))
    (storage : erc20.Erc20.t) :
  let state := State.of_storage storage in
  exists result,
  {{ Environment.of_env env, state |
    ReadMessage.dispatch message ⇓
    (* [inl] means success (no panics) *)
    inl result
  | state }}.
</code></pre><p>This is done by symbolic evaluation of the simulations:</p><pre><code class="language-coq">Proof.
  destruct message; simpl.
  { eexists.
    apply run_total_supply.
  }
  { eexists.
    apply run_balance_of.
  }
  { eexists.
    apply run_allowance.
  }
Qed.
</code></pre><h3>Invariants</h3><p>The data structure of the storage of the smart contract is as follows:</p><pre><code class="language-rust">pub struct Erc20 {
    total_supply: Balance,
    balances: Mapping&lt;AccountId, Balance&gt;,
    allowances: Mapping&lt;(AccountId, AccountId), Balance&gt;,
}
</code></pre><p>An invariant is that the total supply is always equal to the sum of all the balances in the mapping <code>Mapping&lt;AccountId, Balance&gt;</code>. We define this invariant in Coq as:</p><pre><code class="language-coq">Definition sum_of_money (storage : erc20.Erc20.t) : Z :=
  Lib.Mapping.sum Integer.to_Z storage.(erc20.Erc20.balances).

Module Valid.
  Definition t (storage : erc20.Erc20.t) : Prop :=
    Integer.to_Z storage.(erc20.Erc20.total_supply) =
    sum_of_money storage.
End Valid.
</code></pre><p>We show that this invariant holds for any output of the write messages, given that it holds for the input storage:</p><pre><code class="language-coq">Lemma write_dispatch_is_valid
    (env : erc20.Env.t)
    (storage : erc20.Erc20.t)
    (write_message : WriteMessage.t)
    (H_storage : Erc20.Valid.t storage)
    (H_write_message : WriteMessage.Valid.t write_message) :
  let state := State.of_storage storage in
  let &#x27;(result, (storage, _)) :=
    WriteMessage.simulation_dispatch env write_message (storage, []) in
  match result with
  | inl _ =&gt; Erc20.Valid.t storage
  | _ =&gt; True
  end.
</code></pre><p>We assume that the initial storage is valid with the hypothesis:</p><pre><code class="language-coq">(H_storage : Erc20.Valid.t storage)
</code></pre><p>We show the property in the case without panics with:</p><pre><code class="language-coq">match result with
  | inl _ =&gt; ...
</code></pre><p>When the smart contract panics (integer overflow), the storage is discarded anyways, and it might actually by invalid. For example, in the <code>transfer_from_to</code> function we have:</p><pre><code class="language-rust">self.balances.insert(*from, from_balance - value);
let to_balance = self.balance_of_impl(to);
self.balances.insert(*to, to_balance + value);
</code></pre><p>So if there is a panic during the addition<!-- --> <code>+</code>, like an overflow, the final storage can have the <code>from</code> account modified but not the <code>to</code> account. So here, the balance sum is no longer equal to the total supply.</p><h3>Total supply is constant</h3><p>We show that the total supply is also a constant, meaning that no calls to the smart contract can modify its value. The statement is the following:</p><pre><code class="language-coq">Lemma write_dispatch_is_constant
    (env : erc20.Env.t)
    (storage : erc20.Erc20.t)
    (write_message : WriteMessage.t) :
  let state := State.of_storage storage in
  let &#x27;(result, (storage&#x27;, _)) :=
    WriteMessage.simulation_dispatch env write_message (storage, []) in
  match result with
  | inl _ =&gt;
    storage.(erc20.Erc20.total_supply) =
    storage&#x27;.(erc20.Erc20.total_supply)
  | _ =&gt; True
  end.
</code></pre><p>It says that for any initial <code>storage</code> and <code>write_message</code> sent to the smart contract, if we return a result without panicking (<code>inl _</code>), then the total supply in the final storage <code>storage&#x27;</code> is equal to the initial one. We verify this fact by symbolic evaluation of all the branches of the simulation. There are no difficulties in this proof as the code never modifies the <code>total_supply</code>.</p><h3>Action from the logs</h3><p>We infer the action of the smart contract on the storage from its logs. This characterizes exactly what we modifications we can deduce on the storage from the logs. We define an action as a function from the storage to a set of possible new storages, given the knowledge of the logs of the contract:</p><pre><code class="language-coq">Module Action.
  Definition t : Type := erc20.Erc20.t -&gt; erc20.Erc20.t -&gt; Prop.
End Action.
</code></pre><p>The main statement is the following:</p><pre><code class="language-coq">Lemma retrieve_action_from_logs
    (env : erc20.Env.t)
    (storage : erc20.Erc20.t)
    (write_message : WriteMessage.t)
    (events : list erc20.Event.t) :
  match
    WriteMessage.simulation_dispatch env write_message (storage, [])
  with
  | (inl (result.Result.Ok tt), (storage&#x27;, events)) =&gt;
    action_of_events events storage storage&#x27;
  | _ =&gt; True
  end.
</code></pre><p>This relates the final storage <code>storage&#x27;</code> to the initial storage <code>storage</code> using the logs <code>events</code> when there are no panics. We define the <code>action_of_events</code> predicate as the successive application of the <code>action_of_event</code> predicate, which is defined as:</p><pre><code class="language-coq">Definition action_of_event (event : erc20.Event.t) : Action.t :=
  fun storage storage&#x27; =&gt;
  match event with
  | erc20.Event.Transfer (erc20.Transfer.Build_t
      (option.Option.Some from)
      (option.Option.Some to)
      value
    ) =&gt;
    (* In case of transfer event, we do not know how the allowances are
       updated. *)
    exists allowances&#x27;,
    storage&#x27; =
    storage &lt;|
      erc20.Erc20.balances := balances_of_transfer storage from to value
    |&gt; &lt;|
      erc20.Erc20.allowances := allowances&#x27;
    |&gt;
  | erc20.Event.Transfer (erc20.Transfer.Build_t _ _ _) =&gt; False
  | erc20.Event.Approval (erc20.Approval.Build_t owner spender value) =&gt;
    storage&#x27; =
    storage &lt;|
      erc20.Erc20.allowances :=
        Lib.Mapping.insert (owner, spender) value
          storage.(erc20.Erc20.allowances)
    |&gt;
  end.
</code></pre><p>When the <code>event</code> in the logs is of kind <code>erc20.Event.Transfer</code>, the resulting storage has:</p><ul><li>the <code>balances</code> updated according to the function <code>balances_of_transfer</code>;</li><li>the <code>allowances</code> updated to an unknown value <code>allowances&#x27;</code>.</li></ul><p>When the <code>event</code> in the logs is of kind <code>erc20.Event.Approval</code>, the resulting storage has:</p><ul><li>the <code>allowances</code> updated calling <code>Lib.Mapping.insert</code> on <code>(owner, spender)</code>;</li><li>the <code>balances</code> unchanged.</li></ul><h3>Approve only on caller</h3><p>We added one last proof to say that when the <code>approve</code> function succeeds, it only modifies the allowance of the caller:</p><pre><code class="language-coq">Lemma approve_only_changes_owner_allowance
    (env : erc20.Env.t)
    (storage : erc20.Erc20.t)
    (spender : erc20.AccountId.t)
    (value : ltac:(erc20.Balance)) :
  let &#x27;(result, (storage&#x27;, _)) :=
    Simulations.erc20.approve env spender value (storage, []) in
  match result with
  | inl (result.Result.Ok tt) =&gt;
    forall owner spender,
    Integer.to_Z (Simulations.erc20.allowance storage&#x27; owner spender) &lt;&gt;
      Integer.to_Z (Simulations.erc20.allowance storage owner spender) -&gt;
    owner = Simulations.erc20.Env.caller env
  | _ =&gt; True
  end.
</code></pre><p>If an allowance changes after the call to <code>approve</code>, then the owner of the allowance is the caller of the smart contract. This is done by symbolic evaluation of the simulation.</p><h2>Conclusion</h2><p>In this example, we have shown how we formally verify the ERC-20 smart contract written in Rust for the <a href="https://alephzero.org/">Aleph Zero</a> project. Formally verifying smart contracts is extremely important as they can hold a lot of money, and a single bug can prove fatal as recent attacks continue to show: <a href="https://www.ccn.com/education/crypto-hacks-2023-full-list-of-scams-and-exploits-as-millions-go-missing/">List of crypto hacks in 2023</a>.</p><p>If you have Rust smart contracts to verify, feel free to email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>. We will be happy to help!</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Translation of function bodies from Rust to Coq]]></title>
            <link>https://formal.land/blog/2023/11/26/rust-function-body</link>
            <guid>/2023/11/26/rust-function-body</guid>
            <pubDate>Sun, 26 Nov 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[Our tool coq-of-rust enables formal verification of 🦀&nbsp;Rust code, to make sure that a program has no bugs given a precise specification. We work by translating Rust programs to the general proof system 🐓&nbsp;Coq.]]></description>
            <content:encoded><![CDATA[<p>Our tool <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> enables formal verification of <a href="https://www.rust-lang.org/">🦀<!-- --> <!-- -->Rust</a> code, to make sure that a program has no bugs given a precise specification. We work by translating Rust programs to the general proof system <a href="https://coq.inria.fr/">🐓<!-- --> <!-- -->Coq</a>.</p><p>Here, we present how we translate function bodies from Rust to Coq in an example. We also show some of the optimizations we made to reduce the size of the translation.</p><p>:::tip Purchase</p><p>If you need to formally verify your Rust codebase to improve the security of your application, email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>!</p><p>:::</p><p><img src="2023-11-26/rust_and_coq.png" alt="Rust and Coq"/></p><h2>Translating a function body</h2><p>We take the following Rust example as input:</p><pre><code class="language-rust">// fn balance_of_impl(&amp;self, owner: &amp;AccountId) -&gt; Balance { ... }

fn balance_of(&amp;self, owner: AccountId) -&gt; Balance {
    self.balance_of_impl(&amp;owner)
}
</code></pre><p>Here is the corresponding Coq code that <code>coq-of-rust</code> generates <em>without optimizations</em>:</p><pre><code class="language-coq">Definition balance_of
    (self : ref ltac:(Self))
    (owner : erc20.AccountId.t)
    : M ltac:(erc20.Balance) :=
  let* self : M.Val (ref ltac:(Self)) := M.alloc self in
  let* owner : M.Val erc20.AccountId.t := M.alloc owner in
  let* α0 : ref erc20.Erc20.t := M.read self in
  let* α1 : M.Val erc20.Erc20.t := deref α0 in
  let* α2 : ref erc20.Erc20.t := borrow α1 in
  let* α3 : M.Val (ref erc20.Erc20.t) := M.alloc α2 in
  let* α4 : ref erc20.Erc20.t := M.read α3 in
  let* α5 : ref erc20.AccountId.t := borrow owner in
  let* α6 : M.Val (ref erc20.AccountId.t) := M.alloc α5 in
  let* α7 : ref erc20.AccountId.t := M.read α6 in
  let* α8 : M.Val erc20.AccountId.t := deref α7 in
  let* α9 : ref erc20.AccountId.t := borrow α8 in
  let* α10 : M.Val (ref erc20.AccountId.t) := M.alloc α9 in
  let* α11 : ref erc20.AccountId.t := M.read α10 in
  let* α12 : u128.t := erc20.Erc20.t::[&quot;balance_of_impl&quot;] α4 α11 in
  let* α13 : M.Val u128.t := M.alloc α12 in
  M.read α13.
</code></pre><p>This code is much more verbose than the original Rust code as we make all pointer manipulations explicit. We will see just after how to simplify it. We start with the function declaration:</p><pre><code class="language-coq">Definition balance_of
    (self : ref ltac:(Self))
    (owner : erc20.AccountId.t)
    : M ltac:(erc20.Balance) :=
</code></pre><p>that repeats the parameters in the Rust source. Note that the final result is wrapped into the monad type <code>M</code>. This is a monad representing all the side-effects used in Rust programs (state, panic, non-termination, ...). Then, we allocate all the function parameters:</p><pre><code class="language-coq">  let* self : M.Val (ref ltac:(Self)) := M.alloc self in
  let* owner : M.Val erc20.AccountId.t := M.alloc owner in
</code></pre><p>This ensures that both <code>self</code> and <code>owner</code> have an address in memory, in case we borrow them later. This allocation is also fresh, so we cannot access the address of the values from the caller by mistake. We use the monadic let <code>let*</code> as allocations can modify the memory state.</p><p>Then we start by the body of the function itself. We do all the necessary pointer manipulations to compute the parameters <code>self</code> and <code>&amp;owner</code> of the function <code>balance_of_impl</code>. These representations are directly taken from the abstract syntax tree of the Rust compiler (using the <a href="https://rustc-dev-guide.rust-lang.org/thir.html">THIR</a> version).</p><p>For example, for the first parameter <code>self</code>, named <code>α4</code> in this translation, we do:</p><pre><code class="language-coq">  let* α0 : ref erc20.Erc20.t := M.read self in
  let* α1 : M.Val erc20.Erc20.t := deref α0 in
  let* α2 : ref erc20.Erc20.t := borrow α1 in
  let* α3 : M.Val (ref erc20.Erc20.t) := M.alloc α2 in
  let* α4 : ref erc20.Erc20.t := M.read α3 in
</code></pre><p>We combine the operators:</p><ul><li><code>M.read</code>: to get a value of type <code>A</code> from a value with an address <code>M.Val</code>,</li><li><code>deref</code>: to get the value with an address <code>M.Val A</code> pointed by a reference <code>ref A</code>,</li><li><code>borrow</code>: to get the reference <code>ref A</code> to a value with an address <code>M.Val A</code>,</li><li><code>M.alloc</code>: to allocate a new value <code>A</code> in memory, returning a value with address <code>M.Val A</code>.</li></ul><p>We do the same to compute the second parameter <code>&amp;owner</code> of <code>balance_of_impl</code> with:</p><pre><code class="language-coq">  let* α5 : ref erc20.AccountId.t := borrow owner in
  let* α6 : M.Val (ref erc20.AccountId.t) := M.alloc α5 in
  let* α7 : ref erc20.AccountId.t := M.read α6 in
  let* α8 : M.Val erc20.AccountId.t := deref α7 in
  let* α9 : ref erc20.AccountId.t := borrow α8 in
  let* α10 : M.Val (ref erc20.AccountId.t) := M.alloc α9 in
  let* α11 : ref erc20.AccountId.t := M.read α10 in
</code></pre><p>Finally, we call the <code>balance_of_impl</code> function and return the result:</p><pre><code class="language-coq">  let* α12 : u128.t := erc20.Erc20.t::[&quot;balance_of_impl&quot;] α4 α11 in
  let* α13 : M.Val u128.t := M.alloc α12 in
  M.read α13.
</code></pre><p>We do not keep the address of the result, as it will be allocated again by the caller function.</p><h2>Optimizations</h2><p>Some operations can always be removed, namely:</p><ul><li><code>M.read (M.alloc v) ==&gt; v</code>: we do not need to allocate and give an address to a value if it will be immediately read,</li><li><code>deref (borrow v) ==&gt; v</code> and <code>borrow (deref v) ==&gt; v</code>: the borrowing and dereferencing operators are doing the opposite, so they cancel each other. We need to be careful of the mutability status of the borrowing and dereferencing.</li></ul><p>Applying these simple simplification rules, we get the following slimed-down translation:</p><pre><code class="language-coq">Definition balance_of
    (self : ref ltac:(Self))
    (owner : erc20.AccountId.t)
    : M ltac:(erc20.Balance) :=
  let* self : M.Val (ref ltac:(Self)) := M.alloc self in
  let* owner : M.Val erc20.AccountId.t := M.alloc owner in
  let* α0 : ref erc20.Erc20.t := M.read self in
  let* α1 : ref erc20.AccountId.t := borrow owner in
  erc20.Erc20.t::[&quot;balance_of_impl&quot;] α0 α1.
</code></pre><p>This is much shorter and easier to verify!</p><h2>Conclusion</h2><p>We have illustrated in an example how we translate a simple function from Rust to Coq. In this example, we saw how the pointer operations are made explicit in the abstract syntax tree of Rust, and how we simplify them for the frequent cases.</p><p>If you have any comments or suggestions, feel free to email us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>. In future posts, we will go into more detail about the verification process itself.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Optimizing Rust translation to Coq with THIR and bundled traits]]></title>
            <link>https://formal.land/blog/2023/11/08/rust-thir-and-bundled-traits</link>
            <guid>/2023/11/08/rust-thir-and-bundled-traits</guid>
            <pubDate>Wed, 08 Nov 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[We continued our work on coq-of-rust, a tool to formally verify Rust programs using the proof system Coq&nbsp;🐓. This tool translates Rust programs to an equivalent Coq program, which can then be verified using Coq's proof assistant. It opens the door to building mathematically proven bug-free Rust programs.]]></description>
            <content:encoded><![CDATA[<p>We continued our work on <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a>, a tool to formally verify <a href="https://www.rust-lang.org/">Rust</a> programs using the proof system <a href="https://coq.inria.fr/">Coq<!-- --> <!-- -->🐓</a>. This tool translates Rust programs to an equivalent Coq program, which can then be verified using Coq&#x27;s proof assistant. It opens the door to building mathematically proven bug-free Rust programs.</p><p>We present two main improvements we made to <code>coq-of-rust</code>:</p><ul><li>Using the THIR intermediate language of Rust to have more information during the translation to Coq.</li><li>Bundling the type-classes representing the traits of Rust to have faster type-checking in Coq.</li></ul><p><img src="2023-11-08/rust_and_coq.png" alt="Rust and Coq"/></p><h2>THIR intermediate language</h2><p>To translate Rust programs to Coq, we plug into the compiler of Rust, which operates on a series of intermediate languages:</p><ul><li>source code (<code>.rs</code> files);</li><li>abstract syntax tree (AST): immediately after parsing;</li><li><a href="https://rustc-dev-guide.rust-lang.org/hir.html">High-Level Intermediate Representation</a> (HIR): after macro expansion, with name resolution and close to the AST;</li><li><a href="https://rustc-dev-guide.rust-lang.org/thir.html">Typed High-Level Intermediate Representation</a> (THIR): after the type-checking;</li><li><a href="https://rustc-dev-guide.rust-lang.org/mir/index.html">Mid-level Intermediate Representation</a> (MIR): low-level representation based on a <a href="https://en.wikipedia.org/wiki/Control-flow_graph">control-flow graph</a>, inlining traits and polymorphic functions, and with <a href="https://doc.rust-lang.org/book/ch04-02-references-and-borrowing.html">borrow checking</a>;</li><li>machine code (assembly, LLVM IR, ...).</li></ul><p>We were previously using the HIR language to start our translation to Coq, because it is not too low-level and close to what the user has originally in the <code>.rs</code> file. This helps relate the generated Coq code to the original Rust code.</p><p>However, at the level of HIR, there is still a lot of implicit information. For example, Rust has <a href="https://users.rust-lang.org/t/automatic-dereferencing/53828">automatic dereferencing rules</a> that are not yet explicit in HIR. In order not to make any mistakes during our translation to Coq, we prefer to use the next representation, THIR, that makes explicit such rules.</p><p>In addition, the THIR representation shows when a method call is from a trait (and which trait) or from a standalone <code>impl</code> block. Given that we still have trouble translating the traits with <a href="https://coq.inria.fr/doc/V8.18.0/refman/addendum/type-classes.html">type-classes</a> that are inferrable by Coq, this helps a lot.</p><p>A downside of the THIR representation is that it is much more verbose. For example, here is a formatting function generated from HIR:</p><pre><code class="language-coq">Definition fmt
    `{ℋ : State.Trait}
    (self : ref Self)
    (f : mut_ref core.fmt.Formatter)
    : M core.fmt.Result :=
  let* α0 := format_argument::[&quot;new_display&quot;] (addr_of self.[&quot;radius&quot;]) in
  let* α1 :=
    format_arguments::[&quot;new_v1&quot;]
      (addr_of [ &quot;Circle of radius &quot; ])
      (addr_of [ α0 ]) in
  f.[&quot;write_fmt&quot;] α1.
</code></pre><p>This is the kind of functions generated by the <code>#[derive(Debug)]</code> macro of Rust, to implement a formatting function on a type. Here is the version translated from THIR, with explicit borrowing and dereferencing:</p><pre><code class="language-coq">Definition fmt
    `{ℋ : State.Trait}
    (self : ref Self)
    (f : mut_ref core.fmt.Formatter)
    : M ltac:(core.fmt.Result) :=
  let* α0 := deref f core.fmt.Formatter in
  let* α1 := borrow_mut α0 core.fmt.Formatter in
  let* α2 := borrow [ mk_str &quot;Circle of radius &quot; ] (list (ref str)) in
  let* α3 := deref α2 (list (ref str)) in
  let* α4 := borrow α3 (list (ref str)) in
  let* α5 := pointer_coercion &quot;Unsize&quot; α4 in
  let* α6 := deref self converting_to_string.Circle in
  let* α7 := α6.[&quot;radius&quot;] in
  let* α8 := borrow α7 i32 in
  let* α9 := deref α8 i32 in
  let* α10 := borrow α9 i32 in
  let* α11 := core.fmt.rt.Argument::[&quot;new_display&quot;] α10 in
  let* α12 := borrow [ α11 ] (list core.fmt.rt.Argument) in
  let* α13 := deref α12 (list core.fmt.rt.Argument) in
  let* α14 := borrow α13 (list core.fmt.rt.Argument) in
  let* α15 := pointer_coercion &quot;Unsize&quot; α14 in
  let* α16 := core.fmt.Arguments::[&quot;new_v1&quot;] α5 α15 in
  core.fmt.Formatter::[&quot;write_fmt&quot;] α1 α16.
</code></pre><p>We went from a function having two intermediate variables to seventeen intermediate variables. This code is much more verbose, but it is also more explicit. In particular, it details when the:</p><ul><li>borrowing (going from a value of type <code>T</code> to <code>&amp;T</code>), and the</li><li>dereferencing (going from a value of type <code>&amp;T</code> to <code>T</code>)</li></ul><p>occur. It also shows that the method<!-- --> <code>write_fmt</code> is a method from the implementation of the type <code>core.fmt.Formatter</code>, generating:</p><pre><code class="language-coq">core.fmt.Formatter::[&quot;write_fmt&quot;] α1 α16
</code></pre><p>instead of:</p><pre><code class="language-coq">f.[&quot;write_fmt&quot;] α1
</code></pre><h2>Bundled traits</h2><p>Some Rust codebases can have a lot of traits. For example in <a href="https://github.com/paritytech/ink/blob/ccb38d2c3ac27523fe3108f2bb7bffbbe908cdb7/crates/env/src/types.rs#L120">paritytech/ink/crates/env/src/types.rs</a> the trait<!-- --> <code>Environment</code> references more than forty other traits:</p><pre><code class="language-rust">pub trait Environment: Clone {
    const MAX_EVENT_TOPICS: usize;

    type AccountId: &#x27;static
        + scale::Codec
        + CodecAsType
        + Clone
        + PartialEq
        + ...;

    type Balance: &#x27;static
        + scale::Codec
        + CodecAsType
        + ...;

    ...
</code></pre><p>We first used an unbundled approach to represent this trait by a type-class in Coq, as it felt more natural:</p><pre><code class="language-coq">Module Environment.
  Class Trait (Self : Set) `{Clone.Trait Self}
    {AccountId : Set}
    `{scale.Codec.Trait AccountId}
    `{CodecAsType AccountId}
    `{Clone AccountId}
    `{PartialEq AccountId}
    ...
</code></pre><p>However, the backquote operator generated too many implicit arguments, and the type-checker of Coq was very slow. We then switched to a bundled approach, as advocated in this blog post: <a href="https://www.ralfj.de/blog/2019/05/15/typeclasses-exponential-blowup.html">Exponential blowup when using unbundled typeclasses to model algebraic hierarchies</a>. The Coq code for this trait now looks like this:</p><pre><code class="language-coq">Module Environment.
  Class Trait `{ℋ : State.Trait} (Self : Set) : Type := {
    ℋ_0 :: Clone.Trait Self;
    MAX_EVENT_TOPICS : usize;
    AccountId : Set;
    ℒ_0 :: parity_scale_codec.codec.Codec.Trait AccountId;
    ℒ_1 :: ink_env.types.CodecAsType.Trait AccountId;
    ℒ_2 :: core.clone.Clone.Trait AccountId;
    ℒ_3 ::
      core.cmp.PartialEq.Trait AccountId
        (Rhs := core.cmp.PartialEq.Default.Rhs AccountId);
    ...;
    Balance : Set;
    ℒ_8 :: parity_scale_codec.codec.Codec.Trait Balance;
    ℒ_9 :: ink_env.types.CodecAsType.Trait Balance;
    ...;

    ...
</code></pre><p>We use the notation<!-- --> <code>::</code> for fields that are trait instances. With this approach, traits have types as parameters but no other traits.</p><p>The type-checking is now much faster, and in particular, we avoid some cases with exponential blowup or non-terminating type-checking. But this is not a perfect solution as we still have cases where the instance inference does not terminate or fails with hard-to-understand error messages.</p><h2>Conclusion</h2><p>We have illustrated here some improvements we recently made to our <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> translator for two key areas:</p><ul><li>the translation of traits;</li><li>the translation of the implicit borrowing and dereferencing, that can occur every time we call a function.</li></ul><p>These improvements will allow us to formally verify some more complex Rust codebases. In particular, we are applying <code>coq-of-rust</code> to verify smart contracts written for the <a href="https://use.ink/">ink!</a> platform, that is a subset of Rust.</p><p>:::tip Contact</p><p>If you have comments, similar experiences to share, or wish to formally verify your codebase to improve the security of your application, contact us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>!</p><p>:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Trait representation in Coq]]></title>
            <link>https://formal.land/blog/2023/08/25/trait-representation-in-coq</link>
            <guid>/2023/08/25/trait-representation-in-coq</guid>
            <pubDate>Fri, 25 Aug 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[In our project coq-of-rust we translate programs written in Rust to equivalent programs in the language of the proof system Coq&nbsp;🐓, which will later allow us to formally verify them.]]></description>
            <content:encoded><![CDATA[<p>In our project <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> we translate programs written in <a href="https://www.rust-lang.org/">Rust</a> to equivalent programs in the language of the proof system <a href="https://coq.inria.fr/">Coq<!-- --> <!-- -->🐓</a>, which will later allow us to formally verify them.
Both Coq and Rust have many unique features, and there are many differences between them, so in the process of translation we need to treat the case of each language construction separately.
In this post, we discuss how we translate the most complicated one: <a href="https://doc.rust-lang.org/book/ch10-02-traits.html">traits</a>.</p><h2>🦀 Traits in Rust</h2><p>Trait is the way to define a shared behaviour for a group of types in Rust.
To define a trait we have to specify a list of signatures of the methods we want to be implemented for the types implementing our trait.
We can also create a generic definition of a trait with the same syntax as in every Rust definition.
Optionally, we can add a default implementation to any method or extend the list with associated types.
Traits can also extend a behaviour of one or more other traits, in which case, to implement a trait for a type we would have to implement all its supertraits first.</p><p>Consider the following example (adapted from the <a href="https://doc.rust-lang.org/book/">Rust Book</a>):</p><pre><code class="language-rust">struct Sheep {
    naked: bool,
    name: &amp;&#x27;static str,
}

trait Animal {
    // Associated function signature; `Self` refers to the implementor type.
    fn new(name: &amp;&#x27;static str) -&gt; Self;

    // Method signatures; these will return a string.
    fn name(&amp;self) -&gt; &amp;&#x27;static str;
    fn noise(&amp;self) -&gt; &amp;&#x27;static str;

    // Traits can provide default method definitions.
    fn talk(&amp;self) {
        println!(&quot;{} says {}&quot;, self.name(), self.noise());
    }
}

impl Sheep {
    fn is_naked(&amp;self) -&gt; bool {
        self.naked
    }
}

// Implement the `Animal` trait for `Sheep`.
impl Animal for Sheep {
    // `Self` is the implementor type: `Sheep`.
    fn new(name: &amp;&#x27;static str) -&gt; Sheep {
        Sheep {
            name: name,
            naked: false,
        }
    }

    fn name(&amp;self) -&gt; &amp;&#x27;static str {
        self.name
    }

    fn noise(&amp;self) -&gt; &amp;&#x27;static str {
        if self.is_naked() {
            &quot;baaaaah?&quot;
        } else {
            &quot;baaaaah!&quot;
        }
    }

    // Default trait methods can be overridden.
    fn talk(&amp;self) {
        // For example, we can add some quiet contemplation.
        println!(&quot;{} pauses briefly... {}&quot;, self.name, self.noise());
    }
}

impl Sheep {
    fn shear(&amp;mut self) {
        if self.is_naked() {
            // Implementor methods can use the implementor&#x27;s trait methods.
            println!(&quot;{} is already naked...&quot;, self.name());
        } else {
            println!(&quot;{} gets a haircut!&quot;, self.name);

            self.naked = true;
        }
    }
}

fn main() {
    // Type annotation is necessary in this case.
    let mut dolly = Animal::new(&quot;Dolly&quot;): Sheep;

    dolly.talk();
    dolly.shear();
    dolly.talk();
}
</code></pre><p>We have a type <code>Sheep</code>, a trait <code>Animal</code>, and an implementation of <code>Animal</code> for <code>Sheep</code>.
As we can see in <code>main</code>, after a trait is implemented for a type, we can use the methods of the trait like normal methods of the type.</p><h2>Our translation</h2><p>Rust notion of trait is very similar to the concept of <a href="https://en.wikipedia.org/wiki/Type_class">typeclasses</a> in <a href="https://en.wikipedia.org/wiki/Functional_programming">functional programming</a>.
Typeclasses are also present in Coq, so translation of this construction is quite straightforward.</p><p>For a given trait we create a typeclass with fields being just translated signatures of the methods of the trait.
To allow for the use of method syntax, we also define instances of <code>Notation.Dot</code> for every method name of the trait.
We also add a parameter of type <code>Set</code> for every type parameter of the trait and translate trait bounds of the types into equivalent typeclass parameters.</p><h2>Translation of associated types</h2><p>Associated types are a bit harder than methods to translate, because it is possible to use <code>::</code> notation to access them.
For that purpose, we created another typeclass in <code>Notation</code> module:</p><pre><code class="language-coq">Class DoubleColonType {Kind : Type} (type : Kind) (name : string) : Type := {
  double_colon_type : Set;
}.
</code></pre><p>with a notation:</p><pre><code class="language-coq">Notation &quot;e1 ::type[ e2 ]&quot; := (Notation.double_colon_type e1 e2)
  (at level 0).
</code></pre><p>For every associated type, we create a parameter and a field of the typeclass resulting from the trait translation, and below, we create an instance of <code>Notation.DoubleColonType</code>.</p><h2>The example in Coq</h2><p>Here is our Coq translation of the example code above:</p><pre><code class="language-coq">(* Generated by coq-of-rust *)
Require Import CoqOfRust.CoqOfRust.

Module Sheep.
  Unset Primitive Projections.
  Record t : Set := {
    naked : bool;
    name : ref str;
  }.
  Global Set Primitive Projections.

  Global Instance Get_naked : Notation.Dot &quot;naked&quot; := {
    Notation.dot &#x27;(Build_t x0 _) := x0;
  }.
  Global Instance Get_name : Notation.Dot &quot;name&quot; := {
    Notation.dot &#x27;(Build_t _ x1) := x1;
  }.
End Sheep.
Definition Sheep : Set := @Sheep.t.

Module Animal.
  Class Trait (Self : Set) : Set := {
    new `{H : State.Trait} : (ref str) -&gt; (M (H := H) Self);
    name `{H : State.Trait} : (ref Self) -&gt; (M (H := H) (ref str));
    noise `{H : State.Trait} : (ref Self) -&gt; (M (H := H) (ref str));
  }.

  Global Instance Method_new `{H : State.Trait} `(Trait)
    : Notation.Dot &quot;new&quot; := {
    Notation.dot := new;
  }.
  Global Instance Method_name `{H : State.Trait} `(Trait)
    : Notation.Dot &quot;name&quot; := {
    Notation.dot := name;
  }.
  Global Instance Method_noise `{H : State.Trait} `(Trait)
    : Notation.Dot &quot;noise&quot; := {
    Notation.dot := noise;
  }.
  Global Instance Method_talk `{H : State.Trait} `(Trait)
    : Notation.Dot &quot;talk&quot; := {
    Notation.dot (self : ref Self):=
      (let* _ :=
        let* _ :=
          let* α0 := self.[&quot;name&quot;] in
          let* α1 := format_argument::[&quot;new_display&quot;] (addr_of α0) in
          let* α2 := self.[&quot;noise&quot;] in
          let* α3 := format_argument::[&quot;new_display&quot;] (addr_of α2) in
          let* α4 :=
            format_arguments::[&quot;new_v1&quot;]
              (addr_of [ &quot;&quot;; &quot; says &quot;; &quot;
&quot; ])
              (addr_of [ α1; α3 ]) in
          std.io.stdio._print α4 in
        Pure tt in
      Pure tt
      : M (H := H) unit);
  }.
End Animal.

Module Impl_traits_Sheep.
  Definition Self := traits.Sheep.

  Definition is_naked `{H : State.Trait} (self : ref Self) : M (H := H) bool :=
    Pure self.[&quot;naked&quot;].

  Global Instance Method_is_naked `{H : State.Trait} :
    Notation.Dot &quot;is_naked&quot; := {
    Notation.dot := is_naked;
  }.
End Impl_traits_Sheep.

Module Impl_traits_Animal_for_traits_Sheep.
  Definition Self := traits.Sheep.

  Definition new
      `{H : State.Trait}
      (name : ref str)
      : M (H := H) traits.Sheep :=
    Pure {| traits.Sheep.name := name; traits.Sheep.naked := false; |}.

  Global Instance AssociatedFunction_new `{H : State.Trait} :
    Notation.DoubleColon Self &quot;new&quot; := {
    Notation.double_colon := new;
  }.

  Definition name `{H : State.Trait} (self : ref Self) : M (H := H) (ref str) :=
    Pure self.[&quot;name&quot;].

  Global Instance Method_name `{H : State.Trait} : Notation.Dot &quot;name&quot; := {
    Notation.dot := name;
  }.

  Definition noise
      `{H : State.Trait}
      (self : ref Self)
      : M (H := H) (ref str) :=
    let* α0 := self.[&quot;is_naked&quot;] in
    if (α0 : bool) then
      Pure &quot;baaaaah?&quot;
    else
      Pure &quot;baaaaah!&quot;.

  Global Instance Method_noise `{H : State.Trait} : Notation.Dot &quot;noise&quot; := {
    Notation.dot := noise;
  }.

  Definition talk `{H : State.Trait} (self : ref Self) : M (H := H) unit :=
    let* _ :=
      let* _ :=
        let* α0 := format_argument::[&quot;new_display&quot;] (addr_of self.[&quot;name&quot;]) in
        let* α1 := self.[&quot;noise&quot;] in
        let* α2 := format_argument::[&quot;new_display&quot;] (addr_of α1) in
        let* α3 :=
          format_arguments::[&quot;new_v1&quot;]
            (addr_of [ &quot;&quot;; &quot; pauses briefly... &quot;; &quot;
&quot; ])
            (addr_of [ α0; α2 ]) in
        std.io.stdio._print α3 in
      Pure tt in
    Pure tt.

  Global Instance Method_talk `{H : State.Trait} : Notation.Dot &quot;talk&quot; := {
    Notation.dot := talk;
  }.

  Global Instance I : traits.Animal.Trait Self := {
    traits.Animal.new `{H : State.Trait} := new;
    traits.Animal.name `{H : State.Trait} := name;
    traits.Animal.noise `{H : State.Trait} := noise;
  }.
End Impl_traits_Animal_for_traits_Sheep.

Module Impl_traits_Sheep_3.
  Definition Self := traits.Sheep.

  Definition shear `{H : State.Trait} (self : mut_ref Self) : M (H := H) unit :=
    let* α0 := self.[&quot;is_naked&quot;] in
    if (α0 : bool) then
      let* _ :=
        let* _ :=
          let* α0 := self.[&quot;name&quot;] in
          let* α1 := format_argument::[&quot;new_display&quot;] (addr_of α0) in
          let* α2 :=
            format_arguments::[&quot;new_v1&quot;]
              (addr_of [ &quot;&quot;; &quot; is already naked...
&quot; ])
              (addr_of [ α1 ]) in
          std.io.stdio._print α2 in
        Pure tt in
      Pure tt
    else
      let* _ :=
        let* _ :=
          let* α0 := format_argument::[&quot;new_display&quot;] (addr_of self.[&quot;name&quot;]) in
          let* α1 :=
            format_arguments::[&quot;new_v1&quot;]
              (addr_of [ &quot;&quot;; &quot; gets a haircut!
&quot; ])
              (addr_of [ α0 ]) in
          std.io.stdio._print α1 in
        Pure tt in
      let* _ := assign self.[&quot;naked&quot;] true in
      Pure tt.

  Global Instance Method_shear `{H : State.Trait} : Notation.Dot &quot;shear&quot; := {
    Notation.dot := shear;
  }.
End Impl_traits_Sheep_3.

(* #[allow(dead_code)] - function was ignored by the compiler *)
Definition main `{H : State.Trait} : M (H := H) unit :=
  let* dolly :=
    let* α0 := traits.Animal.new &quot;Dolly&quot; in
    Pure (α0 : traits.Sheep) in
  let* _ := dolly.[&quot;talk&quot;] in
  let* _ := dolly.[&quot;shear&quot;] in
  let* _ := dolly.[&quot;talk&quot;] in
  Pure tt.
</code></pre><p>As we can see, the trait <code>Animal</code> is translated to a module <code>Animal</code>. Every time we want to refer to the trait we use the name <code>Trait</code> or <code>Animal.Trait</code>, depending on whether we do it inside or outside its module.</p><h2>Conclusion</h2><p>Traits are similar enough to Coq classes to make the translation relatively intuitive.
The only hard case is a translation of associated types, for which we need a special notation.</p><p>:::tip Contact</p><p>If you have a Rust codebase that you wish to formally verify, or need advice in your work, contact us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>. We will be happy to set up a call with you.</p><p>:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Monad for side effects in Rust]]></title>
            <link>https://formal.land/blog/2023/05/28/monad-for-side-effects-in-rust</link>
            <guid>/2023/05/28/monad-for-side-effects-in-rust</guid>
            <pubDate>Sun, 28 May 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[To formally verify Rust programs, we are building coq-of-rust, a translator from Rust&nbsp;🦀 code to the proof system Coq&nbsp;🐓. We generate Coq code that is as similar as possible to the original Rust code, so that the user can easily understand the generated code and write proofs about it. In this blog post, we explain how we are representing side effects in Coq.]]></description>
            <content:encoded><![CDATA[<p>To formally verify Rust programs, we are building <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a>, a translator from Rust<!-- --> <!-- -->🦀 code to the proof system <a href="https://coq.inria.fr/">Coq<!-- --> <!-- -->🐓</a>. We generate Coq code that is as similar as possible to the original Rust code, so that the user can easily understand the generated code and write proofs about it. In this blog post, we explain how we are representing side effects in Coq.</p><h2>🦀 Side effects in Rust</h2><p>In programming, <a href="https://en.wikipedia.org/wiki/Side_effect_(computer_science)">side effects</a> are all what is not representable by pure functions (mathematical functions, functions that always return the same output for given input parameters). In Rust there are various kinds of side effects:</p><ul><li>errors (the <a href="https://doc.rust-lang.org/core/macro.panic.html">panic!</a> macro) that propagate and do appear in the return type of functions,</li><li>non-termination, with some potentially non-terminating loops (never returning a result is considered as a side-effect),</li><li>control-flow, with the <code>break</code>, <code>continue</code>, <code>return</code> keywords, that can jump to a different part of the code,</li><li>memory allocations and memory mutations,</li><li>I/O, with for example the <a href="https://doc.rust-lang.org/std/macro.println.html">println!</a> macro, that prints a message to the standard output,</li><li>concurrency, with the <a href="https://doc.rust-lang.org/std/thread/fn.spawn.html">thread::spawn</a> function, that creates a new thread.</li></ul><h2>🐓 Coq, a purely functional language</h2><p>Like most proof systems, Coq is a purely functional language. This means we need to find an encoding for the side effects. The reason for most proof systems to forbid side effects is to be logically consistent. Otherwise, it would be easy to write a proof of <code>False</code> by writing a term that does not terminate for example.</p><h2>🔮<!-- --> <!-- -->Monads in Coq</h2><p>Monads are a common way to represent side effects in a functional language. A monad is a type constructor <code>M</code>:</p><pre><code class="language-coq">Definition M (A : Set) : Set :=
  ...
</code></pre><p>representing computations returning values of type <code>A</code>. As an example we can take the error monad of computations that can fail with an error message, using the <a href="https://doc.rust-lang.org/std/result/enum.Result.html">Result</a> type like in Rust:</p><pre><code class="language-coq">Definition M (A : Set) : Set :=
  Result A string.
</code></pre><p>It must have two operators, <code>Pure</code> and <code>Bind</code>.</p><h3>The <code>Pure</code> operator</h3><p>The <code>Pure</code> operator has type:</p><pre><code class="language-coq">Definition Pure {A : Set} (v : A) : M A :=
  ...
</code></pre><p>It lifts a pure value <code>v</code> into the monad. For our error monad, the <code>Pure</code> operator is:</p><pre><code class="language-coq">Definition Pure {A : Set} (v : A) : M A :=
  Ok v.
</code></pre><h3>The <code>Bind</code> operator</h3><p>The <code>Bind</code> operator has type:</p><pre><code class="language-coq">Definition Bind {A B : Set} (e1 : M A) (f : A -&gt; M B) : M B :=
  ...
</code></pre><p>It sequences two computations <code>e1</code> with <code>f</code>, where <code>f</code> is a function that takes the result of <code>e1</code> as input and returns a new computation. We also note the <code>Bind</code> operator:</p><pre><code class="language-coq">let* x := e1 in
e2
</code></pre><p>assuming that <code>f</code> is a function that takes <code>x</code> as input and returns <code>e2</code>. Requiring this operator for all monads shows that sequencing computations is a very fundamental operation for side effects.</p><p>For our error monad, the <code>Bind</code> operator is:</p><pre><code class="language-coq">Definition Bind {A B : Set} (e1 : M A) (f : A -&gt; M B) : M B :=
  match e1 with
  | Ok v =&gt; f v
  | Err msg =&gt; Err msg
  end.
</code></pre><h2>🚧 State, exceptions, non-termination, control-flow</h2><p>We use a single monad to represent all the side effects that interest us in Rust. This monad is called<!-- --> <code>M</code> and is defined as follows:</p><pre><code class="language-coq">Definition RawMonad `{State.Trait} :=
  ...

Module Exception.
  Inductive t (R : Set) : Set :=
  | Return : R -&gt; t R
  | Continue : t R
  | Break : t R
  | Panic {A : Set} : A -&gt; t R.
  Arguments Return {_}.
  Arguments Continue {_}.
  Arguments Break {_}.
  Arguments Panic {_ _}.
End Exception.
Definition Exception := Exception.t.

Definition Monad `{State.Trait} (R A : Set) : Set :=
  nat -&gt; State -&gt; RawMonad ((A + Exception R) * State).

Definition M `{State.Trait} (A : Set) : Set :=
  Monad Empty_set A.
</code></pre><p>We assume the definition of some <code>RawMonad</code> for memory handling that we will describe in a later post. Our monad<!-- --> <code>M</code> is a particular case of the monad<!-- --> <code>Monad</code> with <code>R = Empty_set</code>. It is a combination four monads:</p><ol><li>The <code>RawMonad</code>.</li><li>A state monad, that takes a <code>State</code> as input and a return an updated state as output. The trait <code>State.Trait</code> provides read/write operations on the <code>State</code> type.</li><li>An error monad with errors of type <code>Exception R</code>. There errors include the <code>Return</code>, <code>Continue</code>, <code>Break</code> and <code>Panic</code> constructors. The <code>Return</code> constructor is used to return a value from a function. The <code>Continue</code> constructor is used to continue the execution of a loop. The <code>Break</code> constructor is used to break the execution of a loop. The <code>Panic</code> constructor is used to panic with an error message. We implement all these operations as exceptions, even if only <code>Panic</code> is really an error, as they behave in the same way: interrupting the execution of the current sub-expression to bubble up to a certain level.</li><li>A fuel monad for non-termination, with the additional <code>nat</code> parameter.</li></ol><p>The parameter <code>R</code> of the type constructor <code>Monad</code> is used to represent the type of values that can be returned in the body of a function. It is the same as the return type of the function. So for a function returning a value of type <code>A</code>, we define its body in <code>Monad A A</code>. Then, we wrap it in an operator:</p><pre><code class="language-coq">Definition catch_return {A : Set} (e : Monad A A) : M A :=
  ...
</code></pre><p>that catches the <code>Return</code> exceptions and returns the value.</p><h2>Conclusion</h2><p>We will see in the next post how we define the <code>RawMonad</code> to handle the Rust state of a program and memory allocation.</p><p>:::tip Contact</p><p>If you have a Rust codebase that you wish to formally verify, or need advice in your work, contact us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>. We will be happy to set up a call with you.</p><p>:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Representation of Rust methods in Coq]]></title>
            <link>https://formal.land/blog/2023/04/26/representation-of-rust-methods-in-coq</link>
            <guid>/2023/04/26/representation-of-rust-methods-in-coq</guid>
            <pubDate>Wed, 26 Apr 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[With our project coq-of-rust we aim to translate high-level Rust code to similar-looking Coq code, to formally verify Rust programs. One of the important constructs in the Rust language is the method syntax. In this post, we present our technique to translate Rust methods using type-classes in Coq.]]></description>
            <content:encoded><![CDATA[<p>With our project <a href="https://github.com/formal-land/coq-of-rust">coq-of-rust</a> we aim to translate high-level Rust code to similar-looking <a href="https://coq.inria.fr/">Coq</a> code, to <a href="https://en.wikipedia.org/wiki/Formal_verification">formally verify</a> Rust programs. One of the important constructs in the Rust language is the <a href="https://doc.rust-lang.org/book/ch05-03-method-syntax.html">method syntax</a>. In this post, we present our technique to translate Rust methods using type-classes in Coq.</p><h2>Rust Code To Translate</h2><p>Consider the following Rust example, which contains a method (adapted from the <a href="https://doc.rust-lang.org/book/">Rust Book</a>):</p><pre><code class="language-rust">struct Rectangle {
    width: u32,
    height: u32,
}

impl Rectangle {
    // Here &quot;area&quot; is a method
    fn area(&amp;self) -&gt; u32 {
        self.width * self.height
    }
}

fn main() {
    let rect1 = Rectangle {
        width: 30,
        height: 50,
    };

    println!(
        &quot;The area of the rectangle is {} square pixels.&quot;,
        // We are calling this method there
        rect1.area()
    );
}
</code></pre><p>The Rust compiler can find the implementation of the <code>.area()</code> method call because it knows that the type of <code>rect1</code> is <code>Rectangle</code>. There could be other <code>area</code> methods defined for different types, and the code would still compile calling the <code>area</code> method of <code>Rectangle</code>.</p><p>Coq has no direct equivalent for calling a function based on its name and type.</p><h2>Our Translation</h2><p>Here is our Coq translation of the code above:</p><pre><code class="language-coq"> 1: (* Generated by coq-of-rust *)
 2: Require Import CoqOfRust.CoqOfRust.
 3:
 4: Import Root.std.prelude.rust_2015.
 5:
 6: Module Rectangle.
 7:   Record t : Set := {
 8:     width : u32;
 9:     height : u32;
10:   }.
11:
12:   Global Instance Get_width : Notation.Dot &quot;width&quot; := {
13:     Notation.dot &#x27;(Build_t x0 _) := x0;
14:   }.
15:   Global Instance Get_height : Notation.Dot &quot;height&quot; := {
16:     Notation.dot &#x27;(Build_t _ x1) := x1;
17:   }.
18: End Rectangle.
19: Definition Rectangle : Set := Rectangle.t.
20:
21: Module ImplRectangle.
22:   Definition Self := Rectangle.
23:
24:   Definition area (self : ref Self) : u32 :=
25:     self.[&quot;width&quot;].[&quot;mul&quot;] self.[&quot;height&quot;].
26:
27:   Global Instance Method_area : Notation.Dot &quot;area&quot; := {
28:     Notation.dot := area;
29:   }.
30: End ImplRectangle.
31:
32: Definition main (_ : unit) : unit :=
33:   let rect1 := {| Rectangle.width := 30; Rectangle.height := 50; |} in
34:   _crate.io._print
35:     (_crate.fmt.Arguments::[&quot;new_v1&quot;]
36:       [ &quot;The area of the rectangle is &quot;; &quot; square pixels.\n&quot; ]
37:       [ _crate.fmt.ArgumentV1::[&quot;new_display&quot;] rect1.[&quot;area&quot;] ]) ;;
38:   tt ;;
39:   tt.
</code></pre><p>On line <code>24</code> we define the <code>area</code> function. On line <code>27</code> we declare that <code>area</code> is a method. On line <code>37</code> we call the <code>area</code> method on <code>rect1</code> with:</p><pre><code class="language-coq">rect1.[&quot;area&quot;]
</code></pre><p>which closely resembles the source Rust code:</p><pre><code class="language-rust">rect1.area()
</code></pre><p>Coq can automatically find the code of the <code>area</code> method to call.</p><h2>How It Works</h2><p>The code:</p><pre><code class="language-coq">rect1.[&quot;area&quot;]
</code></pre><p>is actually a notation for:</p><pre><code class="language-coq">Notation.dot &quot;area&quot; rect1
</code></pre><p>Then we leverage the inference mechanism of type-classes in Coq to find the code of the <code>area</code> method:</p><pre><code class="language-coq">Module Notation.
  (** A class to represent the notation [e1.e2]. This is mainly used to call
      methods, or access to named or indexed fields of structures.
      The kind is either a string or an integer. *)
  Class Dot {Kind : Set} (name : Kind) {T : Set} : Set := {
    dot : T;
  }.
  Arguments dot {Kind} name {T Dot}.
End Notation.
</code></pre><p>The <code>Dot</code> class has three parameters: <code>Kind</code>, <code>name</code>, and <code>T</code>. <code>Kind</code> is the type of the name of the method (generally a string but it could be an integer in rare cases), <code>name</code> is the name of the method, and <code>T</code> is the type of the method. The <code>dot</code> field of the class is the code of the method.</p><p>When we define the class instance:</p><pre><code class="language-coq">27:   Global Instance Method_area : Notation.Dot &quot;area&quot; := {
28:     Notation.dot := area;
29:   }.
</code></pre><p>we instantiate the class <code>Notation.Dot</code> with three parameters:</p><ul><li><code>Kind</code> (inferred) is <code>string</code> because the name of the method is a string,</li><li><code>name</code> is <code>&quot;area&quot;</code> because the name of the method is <code>area</code>,</li><li><code>T</code> (inferred) is <code>ref Rectangle -&gt; u32</code> because the method is declared as <code>fn area(&amp;self) -&gt; u32</code>.</li></ul><p>Then we define the <code>dot</code> field of the class instance to be the <code>area</code> function.</p><p>When we call:</p><pre><code class="language-coq">Notation.dot &quot;area&quot; rect1
</code></pre><p>Coq will automatically find the class instance <code>Method_area</code> because the type of <code>rect1</code> is <code>Rectangle</code> and the name of the method is <code>&quot;area&quot;</code>.</p><h2>Other Use Cases</h2><p>The <code>Dot</code> class is also used to access to named or indexed fields of structures or traits. We use a similar mechanism for associated functions. For example, the Rust code:</p><pre><code class="language-rust">let rect1 = Rectangle::square(3);
</code></pre><p>is translated to:</p><pre><code class="language-coq">let rect1 := Rectangle::[&quot;square&quot;] 3 in
</code></pre><p>with a type-class for the <code>type::[name]</code> notation as follows:</p><pre><code class="language-coq">Module Notation.
  (** A class to represent associated functions (the notation [e1::e2]). The
      kind might be [Set] for functions associated to a type,
      or [Set -&gt; Set] for functions associated to a trait. *)
  Class DoubleColon {Kind : Type} (type : Kind) (name : string) {T : Set} :
    Set := {
    double_colon : T;
  }.
  Arguments double_colon {Kind} type name {T DoubleColon}.
End Notation.
</code></pre><h2>In Conclusion</h2><p>The type-classes mechanism of Coq appears flexible enough to represent our current use cases involving methods and associated functions. It remains to be seen whether this approach will suffice for future use cases.</p><p>:::tip Contact</p><p>If you have a Rust codebase that you wish to formally verify, or need advice in your work, contact us at<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a>. We will be happy to set up a call with you.</p><p>:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Current formal verification efforts 💪]]></title>
            <link>https://formal.land/blog/2023/01/24/current-verification-efforts</link>
            <guid>/2023/01/24/current-verification-efforts</guid>
            <pubDate>Tue, 24 Jan 2023 00:00:00 GMT</pubDate>
            <description><![CDATA[We are diversifying ourselves to apply formal verification on 3️⃣ new languages with Solidity, Rust, and TypeScript. In this article we describe our approach. For these three languages, we translate the code to the proof system 🐓&nbsp;Coq. We generate the cleanest&nbsp;🧼 possible output to simplify the formal verification&nbsp;📐 effort that comes after.]]></description>
            <content:encoded><![CDATA[<p>We are diversifying ourselves to apply <a href="https://en.wikipedia.org/wiki/Formal_verification">formal verification</a> on 3️⃣ new languages with <strong>Solidity</strong>, <strong>Rust</strong>, and <strong>TypeScript</strong>. In this article we describe our approach. For these three languages, we translate the code to the proof system <a href="https://coq.inria.fr/">🐓<!-- --> <!-- -->Coq</a>. We generate the cleanest<!-- --> <!-- -->🧼 possible output to simplify the formal verification<!-- --> <!-- -->📐 effort that comes after.</p><blockquote><p>Formal verification is a way to ensure that a program follows its specification in<!-- --> <!-- -->💯% of cases thanks to the use of mathematical methods. It removes far more bugs and security issues than testing, and is necessary to deliver software of the highest quality<!-- --> <!-- -->💎.</p></blockquote><h2>🗺️ General plan</h2><p>To apply formal verification to real-sized applications, we need to handle thousands of lines of code in a seamless way. We rely on the proof system Coq to write our proofs, as it has a mature ecosystem, and automated (SMT) and interactive ways to write proofs. To keep the proofs simple, we must find an efficient way to convert an existing and evolving codebase to Coq.</p><p>For example, given the following TypeScript example:</p><pre><code class="language-typescript">export function checkIfEnoughCredits(user: User, credits: number): boolean {
  if (user.isAdmin) {
    return credits &gt;= 0;
  }

  return credits &gt;= 1000;
}
</code></pre><p>we want to generate the corresponding Coq code in an automated way:</p><pre><code class="language-coq">Definition checkIfEnoughCredits (user : User) (credits : number) : bool :=
  if user.(User.isAdmin) then
    credits &gt;= 0
  else
    credits &gt;= 1000.
</code></pre><p>This is the exact equivalent written using the Coq syntax, where we check the <code>credits</code> condition depending on the user&#x27;s status. This is the <code>checkIfEnoughCredits</code> definition a Coq developer would directly write, in an idiomatic way.</p><p>We make some hypothesis on the input code. In TypeScript we assume the code does not contain mutations, which is often the case to simplify asynchronous code. In Rust we have other hypothesis as making safe mutations is one of the keys features of the language and a frequent pattern. For each language we look for a correct subset to work on, to support common use cases and still generate a clean<!-- --> <!-- -->Coq code.</p><h2>🇸 Solidity</h2><p>➡️ <a href="/docs/verification/solidity">Project page</a> ⬅️</p><p>The <a href="https://soliditylang.org/">Solidity language</a> is the main language to write smart contracts on the <a href="https://ethereum.org/">Ethereum</a> blockchain. As smart contracts cannot be easily updated and handle a large amount of money, it is critical to formally verify them to prevent bugs.</p><p>Our strategy is to develop a translator <a href="https://gitlab.com/formal-land/coq-of-solidity">coq-of-solidity</a> from Solidity to Coq. We are using an implementation of an <a href="https://en.wikipedia.org/wiki/Ethereum#ERC20">ERC-20</a> smart contract as an example to guide our translation. Two top difficulties in the translation of Solidity programs are:</p><ul><li>the use of object-oriented programming with inheritance on classes,</li><li>the use of mutations and errors, that need to be handled in a monad.</li></ul><p>We are still trying various approach to handle these difficulties and generate a clean Coq output for most cases.</p><p>In addition to our work on Solidity, we are looking at the <a href="https://ethereum.org/en/developers/docs/evm/">EVM code</a> that is the assembly language of Ethereum. It has the advantage of being more stable and with a simpler semantics than Solidity. However, it is not as expressive and programs in EVM are much harder to read. We have a prototype of translator from EVM to Coq named <a href="https://gitlab.com/formal-land/ethereum-vm-to-coq">ethereum-vm-to-coq</a>. An interesting goal will be to connect the translation of Solidity and of EVM in Coq to show that they have the same semantics on a given smart contract.</p><p>Note that EVM is the target language of many verification project on Ethereum such as <a href="https://www.certora.com/">Certora</a> or static analyzers. We prefer to target Solidity as it is more expressive and the generated code in Coq will thus be easier to verify.</p><h2>🦀 Rust</h2><p>➡️ <a href="/docs/verification/rust">Project page</a> ⬅️</p><p>The <a href="https://www.rust-lang.org/">Rust language</a> is a modern systems programming language that is gaining popularity. It is a safe language that prevents many common errors such as buffer overflows or use-after-free. It is also a language that is used to write low-level code, such as drivers or operating systems. As such, it is critical to formally verify Rust programs to prevent bugs.</p><p>We work in collaboration with the team developing the <a href="https://github.com/AeneasVerif">Aeneas</a> project, with people from Inria and Microsoft. The aim is to translate Rust code with mutations to a purely functional form in Coq (without mutations) to simplify the verification effort and avoid the need of separation logic. The idea of this translation is explained in the <a href="https://dl.acm.org/doi/abs/10.1145/3547647">Aeneas paper</a>.</p><p>There are two steps in the translation:</p><ol><li><strong>From <a href="https://rustc-dev-guide.rust-lang.org/mir/index.html">MIR</a> (low-level intermediate form of Rust) to LLBC.</strong> This is a custom language for the project that contains all the information of MIR but is better suited for analysis. For example, instead of using a control-flow graph it uses control structures and an abstract syntax tree. This step is implemented in Rust.</li><li><strong>From LLBC to Coq.</strong> This is the heart of the project and is implemented in OCaml. This is where the translation from mutations to a purely functional form occurs.</li></ol><p>For now we are focusing on adding new features to LLBC and improving the user experience: better error messages, generation of an output with holes for unhandled Rust features.</p><h2>🌐 TypeScript</h2><p>➡️ <a href="/docs/verification/typescript">Project page</a> ⬅️</p><p>We have a <a href="https://formal-land.github.io/coq-of-js/">📽️<!-- --> <!-- -->demo project</a> to showcase the translation of a purely functional subset of JavaScript to Coq. We handle functions and basic data types such as records, enums and discriminated unions. We are now porting the code to TypeScript in <a href="https://github.com/formal-land/coq-of-ts">coq-of-ts</a>. We prefer to work on TypeScript rather than JavaScript as type information are useful to guide the translation, and avoid the need of additional annotations on the source code.</p><p>Our next target will be to make <code>coq-of-ts</code> usable on real-life project example.</p><p>:::info Social media
Follow us on Twitter at <a href="https://twitter.com/LandFoobar">Twitter</a> 🐦 and <a href="https://t.me/formal_land">Telegram</a> to get the latest news about our projects. If you think our work is interesting, please share it with your friends and colleagues. 🙏
:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Latest blog posts on our formal verification effort on Tezos]]></title>
            <link>https://formal.land/blog/2022/12/13/latest-blog-posts-on-tezos</link>
            <guid>/2022/12/13/latest-blog-posts-on-tezos</guid>
            <pubDate>Tue, 13 Dec 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[Here we recall some blog articles that we have written since this summer, on the formal verification of the protocol of Tezos. For this project, we are verifying a code base of around 100,000 lines of OCaml code. We automatically convert the OCaml code to the proof system Coq using the converter coq-of-ocaml. We then apply various proof techniques to make sure that the protocol of Tezos does not contain bugs.]]></description>
            <content:encoded><![CDATA[<p>Here we recall some blog articles that we have written since this summer, on the <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/">formal verification of the protocol of Tezos</a>. For this project, we are verifying a code base of around 100,000 lines of OCaml code. We automatically convert the OCaml code to the proof system Coq using the converter <a href="https://github.com/formal-land/coq-of-ocaml">coq-of-ocaml</a>. We then apply various proof techniques to make sure that the protocol of Tezos does not contain bugs.</p><h2>Blog articles 📝</h2><p>Here is the list of articles about the work we have done since this summer. We believe that some of this work is very unique and specific to Tezos.</p><ul><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/12/12/internal-errors-step-by-step/">The error monad, internal errors and validity predicates, step-by-step</a> by <em>Pierre Vial</em>: a detailed explanation of what we are doing to verify the absence of unexpected errors in the whole code base;</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/10/18/absence-of-internal-errors/">Absence of internal errors</a> by <em>Guillaume Claret</em>: the current state of our proofs to verify the absence of unexpected errors;</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/10/03/verifying-the-skip-list-inductive-predicates/">Skip-list verification. Using inductive predicates</a> by <em>Bartłomiej Królikowski</em> and <em>Natalie Klaus</em>: a presentation of our verification effort on the skip-list algorithm implementation (part 2);</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/10/03/verifying-the-skip-list/">Verifying the skip-list</a> by <em>Natalie Klaus</em> and <em>Bartłomiej Królikowski</em>: a presentation of our verification effort on the skip-list algorithm implementation (part 1);</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/08/15/verify-json-data-encoding/">Verifying json-data-encoding</a> by <em>Tait van Strien</em>: our work to verify an external library used by the Tezos protocol, to safely serialize data to JSON values;</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/07/19/fixing-proofs/">Fixing reused proofs</a> by <em>Bartłomiej Królikowski</em>: a presentation, with examples, of the work we do to maintain existing proofs and specifications as the code evolves;</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/06/07/formal-verification-of-property-based-tests/">Formal verification of property based tests</a> by <em>Guillaume Claret</em>: the principle and status of our work to formally verify the generalized case of property-based tests;</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/06/02/plan-backward-compatibility">Plan for backward compatibility verification</a> by <em>Guillaume Claret</em>: an explanation of the strategy we use to show that two successive versions of the Tezos protocol are fully backward compatible.</li></ul><p>To follow more of our activity, feel free to register on our <a href="https://twitter.com/LandFoobar">Twitter account 🐦</a>! If you need services or advices to formally verify your code base, you can drop us an <a href="mailto:contact@formal.land">email 📧</a>!</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Upgrade coq-of-ocaml to OCaml 4.14]]></title>
            <link>https://formal.land/blog/2022/06/23/upgrade-coq-of-ocaml-4.14</link>
            <guid>/2022/06/23/upgrade-coq-of-ocaml-4.14</guid>
            <pubDate>Thu, 23 Jun 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[In an effort to support the latest version of the protocol of Tezos we upgraded coq-of-ocaml to add compatibility with OCaml 4.14. The result is available in the branch ocaml-4.14. We describe here how we made this upgrade.]]></description>
            <content:encoded><![CDATA[<p>In an effort to support the latest version of the <a href="https://gitlab.com/tezos/tezos/-/tree/master/src/proto_alpha/lib_protocol">protocol of Tezos</a> we upgraded <a href="https://github.com/formal-land/coq-of-ocaml"><code>coq-of-ocaml</code></a> to add compatibility with OCaml 4.14. The result is available in the branch <a href="https://github.com/formal-land/coq-of-ocaml/pull/217"><code>ocaml-4.14</code></a>. We describe here how we made this upgrade.</p><h2>Usage of Merlin</h2><p>In <code>coq-of-ocaml</code> we are using <a href="https://github.com/ocaml/merlin">Merlin</a> to get the typed <a href="https://en.wikipedia.org/wiki/Abstract_syntax_tree">abstract syntax tree</a> of OCaml files. We see the AST through the <a href="https://docs.mirage.io/ocaml/Typedtree/index.html">Typedtree</a> interface, together with an access to all the definitions of the current compilation environment. Merlin computes the current environment by understanding how an OCaml project is configured and connecting to the <a href="https://dune.build/">dune</a> build system. The environment is mandatory for certain transformations in <code>coq-of-ocaml</code>, like:</p><ul><li>finding a canonical name for module types;</li><li>propagating phantom types.</li></ul><p>In order to use Merlin as a library (rather than as a daemon), we vendor the <a href="https://github.com/rgrinberg/merlin/tree/lsp">LSP version</a> of <a href="https://github.com/rgrinberg">rgrinberg</a> in the folder <a href="https://github.com/formal-land/coq-of-ocaml/tree/master/vendor"><code>vendor/</code></a>. This vendored version works with no extra configurations.</p><h2>Upgrade</h2><p>When a new version of OCaml is out, we upgrade our vendored version of Merlin to a compatible one. Then we do the necessary changes to <code>coq-of-ocaml</code>, as the interface of the AST generally evolves with small changes. For OCaml 4.14, the main change was some types becoming abstract such as <code>Types.type_expr</code>. To access to the fields of these types, we now need to use a specific getter and do changes such as:</p><pre><code class="language-diff">+    match typ.desc with
-    match Types.get_desc typ with
</code></pre><p>This made some patterns in <code>match</code> expressions more complex, but otherwise the changes were very minimal. We ran all the unit-tests of <code>coq-of-ocaml</code> after the upgrade and they were still valid.</p><h2>Git submodule or copy &amp; paste?</h2><p>To vendor Merlin we have two possibilities:</p><ol><li>Using a <a href="https://git-scm.com/book/en/v2/Git-Tools-Submodules">Git submodule</a>.</li><li>Doing a copy &amp; paste of the code.</li></ol><p>The first possibility is more efficient in terms of space, but there are a few disadvantages:</p><ul><li>we cannot make small modifications if needed;</li><li>the archives generated by Github do not contain the code of the submodules (see this <a href="https://github.com/dear-github/dear-github/issues/214">issue</a>)</li><li>if a commit in the repository for the submodule disappears, then the submodule is unusable.</li></ul><p>The last reason forced us to do a copy &amp; paste for OCaml 4.14. We now have to be cautious not to commit the generate <code>.ml</code> file for the OCaml parser.</p><h2>Next</h2><p>The next change will be doing the upgrade to OCaml 5. There should be much more changes, and in particular a new way of handling the effects. We do not know yet if it will be possible to translate the effect handlers to Coq in a nice way.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Status update on the verification of Tezos]]></title>
            <link>https://formal.land/blog/2022/06/15/status update-tezos</link>
            <guid>/2022/06/15/status update-tezos</guid>
            <pubDate>Wed, 15 Jun 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[Here we give an update on our verification effort on the protocol of Tezos. We add the marks:]]></description>
            <content:encoded><![CDATA[<p>Here we give an update on our <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/">verification effort</a> on the protocol of Tezos. We add the marks:</p><ul><li>✅ for &quot;rather done&quot;</li><li>🌊 for &quot;partially done&quot;</li><li>❌ for &quot;most is yet to do&quot;</li></ul><p>On the website of project, we also automatically generates pages such as <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/status/compare/">Compare</a> to follow the status of the tasks.</p><h2>Maintenance of the translation ✅</h2><p>We were able to maintain most of the translation from OCaml to Coq of the protocol of Tezos using <a href="https://github.com/formal-land/coq-of-ocaml">coq-of-ocaml</a>, including all the translation of the Michelson interpreter. There was an increase in the size of the OCaml code base in recent months, due to new features added in Tezos like the <a href="https://research-development.nomadic-labs.com/tezos-is-scaling.html">rollups</a>. Here are the numbers of lines of code (<code>.ml</code> and <code>.mli</code> files) for the various protocol versions:</p><ul><li>protocol H: <code>51147</code></li><li>protocol I: <code>59535</code></li><li>protocol J: <code>83271</code> (increase mainly due to the rollups)</li><li>protocol Alpha (development version of K): <code>90716</code></li></ul><p>We still translate most of the protocol code up to version J. We stayed on version J for a while as we wanted to add as many proofs as possible before doing a proof of backward compatibility between J and K. We are currently updating the translation to support the protocol version Alpha, preparing for the translation of K.</p><p>For protocol J, we needed to add a <a href="https://gitlab.com/nomadic-labs/coq-tezos-of-ocaml/-/blob/master/blacklist.txt">blacklist.txt</a> of files that we do not support. Indeed, we need to add new changes to <code>coq-of-ocaml</code> to support these or do hard-to-maintain changes to <a href="https://gitlab.com/tezos/tezos/-/merge_requests/3303">our fork</a> of the Tezos protocol. We plan to complete the translation and remove this black-list for the protocol J soon (in a week or two).</p><h2>Size of the proofs ✅</h2><p>One of our plans is to have a reasonable quantity of proofs, to cover a reasonable quantity of code and properties from the protocol. We believe we have a good quantity of proofs now, as we have more than 50,000 lines of Coq code (for an OCaml codebase of 80,000 lines).</p><p>In addition to our main targets, we verify many &quot;smaller&quot; properties, such as:</p><ul><li>conversion functions are inverses (when there are two <code>to_int</code> and <code>of_int</code> functions in a file, we show that they are inverses);</li><li>the <code>compare</code> functions, to order elements, are well defined (see our blog post <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/04/04/verifying-the-compare-functions">Verifying the compare functions of OCaml</a>);</li><li>invariants are preserved. For example, <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/proofs/carbonated_map#Make.update_is_valid">here</a> we show that updating a carbonated map preserves the property of having a size field actually equal to the number of elements.</li></ul><p>We should note that the size of Coq proofs tends to grow faster than the size of the verified code. We have no coverage metrics to know how much of the code is covered by these proofs.</p><h2>Data-encodings 🌊</h2><p>The <a href="https://gitlab.com/nomadic-labs/data-encoding">data-encoding</a> library is a set of combinators to write serialization/de-serialization functions. We verify that the encodings defined for each protocol data type are bijective. The good thing we have is a semi-automated tactic to verify the use of the <code>data-encoding</code> primitives. We detail this approach in our blog post <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2021/11/22/data-encoding-automation">Automation of <code>data_encoding</code> proofs</a>. We can verify most of the encoding functions that we encounter. From there, we also express the <strong>invariant</strong> associated with each data type, which the encodings generally check at runtime. The invariants are then the domain of definition of the encodings.</p><p>However, we have a hole: we do not verify the <code>data-encoding</code> library itself. Thus the <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/environment/proofs/data_encoding">axioms we made</a> on the data-encoding primitives may have approximations. And indeed, we missed one issue in the development code of the protocol. This is thus a new high-priority target to verify the <code>data-encoding</code> library itself. One of the challenges for the proof is the use of side-effects (references and exceptions) in this library.</p><h2>Property-based tests 🌊</h2><p>The property-based tests on the protocol are located in <a href="https://gitlab.com/tezos/tezos/-/tree/master/src/proto_alpha/lib_protocol/test/pbt"><code>src/proto_alpha/lib_protocol/test/pbt</code></a>. These tests are composed of:</p><ul><li>a generator, generating random inputs of a certain shape;</li><li>a property function, a boolean function taking a generated input and supposed to always answer <code>true</code>.</li></ul><p>We translated a part of these tests to Coq, to convert them to theorems and have specifications extracted from the code. The result of this work is summarized in this blog post: <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2022/06/07/formal-verification-of-property-based-tests">Formal verification of property based tests</a>. We have fully translated and verified four test files over a total of twelve. We are continuing the work of translations and proofs.</p><p>However, we found that for some of the files the proofs were taking a long time to write compared to the gains in safety. Indeed, the statements made in the tests are sometimes too complex when translated into general theorems. For example, for <a href="https://gitlab.com/tezos/tezos/-/blob/master/src/proto_alpha/lib_protocol/test/pbt/test_carbonated_map.ml">test_carbonated_map.ml</a> we have to deal with:</p><ul><li>gas exhaustion (seemingly impossible in the tests);</li><li>data structures of size greater than <code>max_int</code> (impossible in practice).</li></ul><p>All of that complicate the proofs for little gain in safety. So I would say that not all the property-based tests have a nice and useful translation to Coq. We should still note that for some of the tests, like with saturation arithmetic, we have proofs that work well. For these, we rely on the automated linear arithmetic tactic <a href="https://coq.inria.fr/refman/addendum/micromega.html"><code>lia</code></a> of Coq to verify properties over integer overflows.</p><h2>Storage system 🌊</h2><p>By &quot;storage system&quot; we understand the whole set of functors defined in <a href="https://gitlab.com/tezos/tezos/-/blob/master/src/proto_alpha/lib_protocol/storage_functors.ml"><code>storage_functors.ml</code></a> and how we apply them to define the protocol storage in <a href="https://gitlab.com/tezos/tezos/-/blob/master/src/proto_alpha/lib_protocol/storage_functors.ml"><code>storage.ml</code></a>. These functors create sub-storages with signatures such as:</p><pre><code class="language-ocaml">module type Non_iterable_indexed_data_storage = sig
  type t
  type context = t
  type key
  type value
  val mem : context -&gt; key -&gt; bool Lwt.t
  val get : context -&gt; key -&gt; value tzresult Lwt.t
  val find : context -&gt; key -&gt; value option tzresult Lwt.t
  val update : context -&gt; key -&gt; value -&gt; Raw_context.t tzresult Lwt.t
  val init : context -&gt; key -&gt; value -&gt; Raw_context.t tzresult Lwt.t
  val add : context -&gt; key -&gt; value -&gt; Raw_context.t Lwt.t
  val add_or_remove : context -&gt; key -&gt; value option -&gt; Raw_context.t Lwt.t
  val remove_existing : context -&gt; key -&gt; Raw_context.t tzresult Lwt.t
  val remove : context -&gt; key -&gt; Raw_context.t Lwt.t
end
</code></pre><p>This <code>Non_iterable_indexed_data_storage</code> API looks like the API of an OCaml&#x27;s <a href="https://v2.ocaml.org/api/Map.Make.html">Map</a>. As a result, our goal for the storage is to show that is can be simulated by standard OCaml data structures such as sets and maps. This is a key step to unlock further reasoning about code using the storage.</p><p>Unfortunately, we were not able to verify the whole storage system yet. Among the difficulties are that:</p><ul><li>there are many layers in the definition of the storage;</li><li>the storage functors use a lot of abstractions, and sometimes it is unclear how to specify them in the general case.</li></ul><p>Still, we have verified some of the functors as seen in <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/proofs/storage_functors"><code>Proofs/Storage_functors.v</code></a> and specified the <code>storage.ml</code> file in <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/storage"><code>Proos/Storage.v</code></a>. We believe in having the correct specifications for all of the storage abstractions now. We plan to complete all these proofs later.</p><h2>Michelson</h2><p>The verification of the Michelson interpreter is what occupied most of our time. By considering the OCaml files whose name starts by <code>script_</code>, the size of the Michelson interpreter is around 20,000 lines of OCaml code.</p><h3>Simulations 🌊</h3><p>The interpreter relies heavily on <a href="https://v2.ocaml.org/manual/gadts.html">GADTs</a> in OCaml. Because these do not translate nicely in Coq, we need to write simulations in dependent types of the interpreter functions, and prove them correct in Coq. We describe this process in our <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/guides/michelson">Michelson Guide</a>.</p><p>The main difficulties we encountered are:</p><ul><li>the number of simulations to write (covering the 20,000 lines of OCaml);</li><li>the execution time of the proof of correctness of the simulations. This is due to the large size of the inductive types describing the Michelson AST, and the use of dependent types generating large proof terms. For example, there are around 30 cases for the types and 150 for the instructions node in the AST.</li></ul><p>When writing the simulations, we are also verifying the termination of all the functions and the absence of reachable <code>assert false</code>. We have defined the simulation of many functions, but are still missing important ones such as <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/script_ir_translator/#parse_instr_aux"><code>parse_instr_aux</code></a> to parse Michelson programs.</p><h3>Mi-Cho-Coq 🌊</h3><p>We have a project to verify that the <a href="https://gitlab.com/nomadic-labs/mi-cho-coq">Mi-Cho-Coq</a> framework, used to formally verify smart contracts written in Michelson, is compatible with the implementation of the Michelson interpreter in OCaml. We have a partial proof of compatibility in <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/docs/simulations/micho_to_dep">Micho_to_dep.v</a>. We still need to complete this proof, especially to handle instructions with loops. Our goal is to show a complete inclusion of the semantics of Mi-Cho-Coq into the semantics of the implementation.</p><h3>Parse/unparse ❌</h3><p>We wanted to verify that the various parsing and unparsing functions over Michelson are inverses. These functions exist for:</p><ul><li>comparable types</li><li>types</li><li>comparable data</li><li>data</li></ul><p>Because we are still focused on writing, verifying or updating the simulations, we are still not done for this task.</p><h2>Conclusion</h2><p>We have many ongoing projects but few fully completed tasks. We will focus more on having terminated proofs.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Make Tezos the first formally verified cryptocurrency]]></title>
            <link>https://formal.land/blog/2022/02/02/make-tezos-a-formally-verified-crypto</link>
            <guid>/2022/02/02/make-tezos-a-formally-verified-crypto</guid>
            <pubDate>Wed, 02 Feb 2022 00:00:00 GMT</pubDate>
            <description><![CDATA[Elephants]]></description>
            <content:encoded><![CDATA[<p><img src="elephants-elmira-gokoryan.webp" alt="Elephants"/></p><p>Our primary goal at <a href="https://formal.land/">Formal<!-- --> <!-- -->Land<!-- --> <!-- -->🌲</a> is to make <a href="https://tezos.com/">Tezos</a> the first crypto-currency with a formally verified implementation. With <a href="https://en.wikipedia.org/wiki/Formal_verification">formal verification</a>, thanks to mathematical methods, we can check that a program behaves as expected for all possible inputs. Formal verification goes beyond what testing can do, as testing can only handle a finite amount of cases. That is critical as cryptocurrencies hold a large amount of money (around $3B for Tezos today). The current result of our verification project is available on <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/">nomadic-labs.gitlab.io/coq-tezos-of-ocaml</a>. Formal verification is also key to allowing Tezos to evolve constantly in a safe and backward compatible manner.</p><p>We proceed in two steps:</p><ol><li>we translate the code of Tezos, written in <a href="https://ocaml.org/">OCaml</a>, to the proof language <a href="https://coq.inria.fr/">Coq</a> using the translator <a href="https://github.com/foobar-land/coq-of-ocaml">coq-of-ocaml</a>;</li><li>we write our specifications and proofs in the Coq language.</li></ol><p>We believe this is one of the most efficient ways to proceed, as we can work on an almost unmodified version of the codebase and use the full power of the mature proof system Coq. The code of Tezos is composed of around:</p><ul><li>50,000 lines for the protocol (the kernel of Tezos), and</li><li>200,000 lines for the shell (everything else, including the peer-to-peer layer and the storage backend).</li></ul><p>We are currently focusing on verifying the protocol for the following modules.</p><h2>Data-encoding</h2><p>The <a href="https://gitlab.com/nomadic-labs/data-encoding">data-encoding</a> library offers serialization and deserialization to binary and JSON formats. It is used in various parts of the Tezos protocol, especially on all the data types ending up in the storage system. In practice, many encodings are defined in the OCaml files named <code>*_repr.ml</code>. We verify that the <code>data-encoding</code> library is correctly used to define the encodings. We check that converting a value to binary format and from binary returns the initial value. We explicit the domain of validity of such conversions. This verification work generally reveals and propagates invariants about the data structures of the protocol. As an invariant example, all the account amounts should always be positive. Having these invariants will be helpful for the verification of higher-level layers of the protocol.</p><h2>Michelson smart contracts</h2><p>The smart contract language of Tezos is <a href="https://tezos.gitlab.io/active/michelson.html">Michelson</a>. The interpreter and type-checker of smart contracts is one of the most complex and critical parts of the protocol. We are verifying two things about this code:</p><ul><li>The equivalence of the interpreter and the Coq semantics for Michelson defined in the project <a href="https://gitlab.com/nomadic-labs/mi-cho-coq">Mi-Cho-Coq</a>. Thanks to this equivalence, we can make sure that the formal verification of smart contracts is sound for the current version of the protocol.</li><li>The compatibility of the parsing and unparsing functions for the Michelson types and values. The parsing functions take care of the type-checking and do a lot of sanity checks on Michelson expressions with appropriate error messages. Showing that the parsing and unparsing functions are inverses is important for security reasons. The Michelson values are always unparsed at the end of a smart contract execution to be stored on disk.</li></ul><p>To do these proofs, we also give a new semantics of Michelson, expressed using dependent types rather than <a href="https://ocaml.org/manual/gadts-tutorial.html">GADTs</a> in the OCaml implementation.</p><h2>Storage system</h2><p>Cryptocurrencies typically take a lot of space on disk (in the hundreds of gigabytes). In Tezos, we use the key-value database <a href="https://irmin.org/">Irmin</a>. The protocol provides a lot of <a href="https://gitlab.com/tezos/tezos/-/blob/master/src/proto_alpha/lib_protocol/storage_functors.ml">abstractions</a> over this database to expose higher-level interfaces with set and map-like APIs. We verify that these abstractions are valid doing a proof by simulation, where we show that the whole system is equivalent to an <a href="https://en.wikipedia.org/wiki/In-memory_database">in-memory database</a> using simpler data structures. Thanks to this simulation, we will be able to reason about code using the storage as if we were using the simpler in-memory version.</p><h2>In addition</h2><p>We also plan to verify:</p><ul><li>The implementation of the <code>data-encoding</code> library itself. This code is challenging for formal verification as it contains many imperative features. Another specificity of this library is that it sits outside of the protocol of Tezos, and we might need to adapt <code>coq-of-ocaml</code> to support it.</li><li>The <a href="https://gitlab.com/tezos/tezos/-/tree/master/src/proto_alpha/lib_protocol/test/pbt">property-based tests of the protocol</a>. These tests are written as boolean functions (or functions raising exceptions), which must return <code>true</code> on any possible inputs. We will verify them in the general case by importing their definitions to Coq and verifying with mathematical proofs that they are always correct.</li></ul><p>:::tip Contact
For any questions or remarks, contact us on 👉<!-- --> <a href="mailto:contact@formal.land">c<!-- -->o<!-- -->n<!-- -->t<!-- -->a<!-- -->c<!-- -->t<!-- -->@<!-- -->formal<!-- -->.<!-- -->l<!-- -->a<!-- -->n<!-- -->d</a> <!-- -->👈.
:::</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[New blog posts and Meetup talk]]></title>
            <link>https://formal.land/blog/2021/11/12/new-blog-posts-and-meetup-talk</link>
            <guid>/2021/11/12/new-blog-posts-and-meetup-talk</guid>
            <pubDate>Fri, 12 Nov 2021 00:00:00 GMT</pubDate>
            <description><![CDATA[Recently, we added two new blog posts about the verification of the crypto-currency Tezos:]]></description>
            <content:encoded><![CDATA[<p>Recently, we added two new blog posts about the verification of the crypto-currency <a href="https://tezos.com/">Tezos</a>:</p><ul><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2021/11/01/verify-michelson-types-mi-cho-coq/">Verify the Michelson types of Mi-Cho-Coq</a> to compare the types defined in the Tezos code for the <a href="http://tezos.gitlab.io/active/michelson.html">Michelson</a> interpreter and in the <a href="https://gitlab.com/nomadic-labs/mi-cho-coq">Mi-Cho-Coq library</a> to verify smart contracts;</li><li><a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2021/11/08/translate-tenderbake/">Translate the Tenderbake&#x27;s code to Coq</a> to explain how we translated the recent changes in Tezos to the Coq using <a href="https://github.com/foobar-land/coq-of-ocaml">coq-of-ocaml</a>. In particular we translated the code of the new <a href="https://research-development.nomadic-labs.com/a-look-ahead-to-tenderbake.html">Tenderbake</a> consensus algorithm.</li></ul><p>We also talked at the <a href="https://www.meetup.com/LambdaLille/events/281374644/">Lambda Lille Meetup</a> (in French) to present our work on <code>coq-of-ocaml</code> for Tezos. A video on the <a href="https://www.youtube.com/channel/UC-hC7y_ilQBq0QCa9xDu1iA">Youtube channel</a> of the Meetup should be available shortly. We thanks the organizers for hosting the talk.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Verification of the use of data-encoding]]></title>
            <link>https://formal.land/blog/2021/10/27/verification-data-encoding</link>
            <guid>/2021/10/27/verification-data-encoding</guid>
            <pubDate>Wed, 27 Oct 2021 00:00:00 GMT</pubDate>
            <description><![CDATA[We added a blog post about the verification of the use of data-encodings in the protocol of Tezos. Currently, we work on the verification of Tezos and publish our blog articles there. We use coq-of-ocaml to translate the OCaml code to Coq and do our verification effort.]]></description>
            <content:encoded><![CDATA[<p>We added a blog post about the <a href="https://formal-land.gitlab.io/coq-tezos-of-ocaml/blog/2021/10/20/data-encoding-usage">verification of the use of data-encodings</a> in the protocol of Tezos. Currently, we work on the verification of Tezos and publish our blog articles there. We use <a href="https://foobar-land.github.io/coq-of-ocaml/">coq-of-ocaml</a> to translate the OCaml code to Coq and do our verification effort.</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Welcome]]></title>
            <link>https://formal.land/blog/2021/10/10/welcome</link>
            <guid>/2021/10/10/welcome</guid>
            <pubDate>Sun, 10 Oct 2021 00:00:00 GMT</pubDate>
            <description><![CDATA[Welcome to the blog of Formal Land. Here we will post various updates about the work we are doing.]]></description>
            <content:encoded><![CDATA[<p>Welcome to the blog of <a href="/">Formal Land</a>. Here we will post various updates about the work we are doing.</p>]]></content:encoded>
        </item>
    </channel>
</rss>